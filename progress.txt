# adgate progress log
# One entry per completed story. Newest at the bottom. Format:
#
# ## <YYYY-MM-DD> <story id> <title>
# Built: one or two lines
# Decisions: choices the next iteration must respect
# Gotchas: anything that cost time
# Ideas: out of scope improvements (do not implement)
#
# If blocked: a single line "NEEDS HUMAN: <story id> <reason>" and stop.

## Codebase notes (maintained across iterations, edit in place)
- Docker Postgres host port is ${PGPORT_HOST:-5432} in docker-compose.yml. On this dev machine a native Postgres owns 5432, so .env sets PGPORT_HOST=5433 and both DATABASE_URL values use 5433. CI and fresh clones keep 5432. Always read the port from env, never hardcode 5432.
- CI is .github/workflows/ci.yml: ubuntu-latest, Node 20, pnpm from package.json packageManager (pnpm/action-setup@v4 with no version input), `pnpm install --frozen-lockfile`, then typecheck, lint, test. A postgres:16 service container (user/password/db adgate) listens on localhost:5432 and the job env exports DATABASE_URL and DATABASE_URL_TEST on 5432. Service containers cannot mount scripts/db/init.sql, so a psql step creates adgate_test (idempotent). Integration tests must read DATABASE_URL_TEST from the environment and truncate tables; they must not assume the compose init script ran.
- .env.example is the canonical list of env vars, one comment line per variable directly above it. When a story adds a variable, add it there with a comment (and to the CI job env if tests need it).
- LICENSE is the canonical Apache 2.0 text from apache.org (11358 bytes). Never edit it; add third-party notices in a separate NOTICE file if ever needed.
- @adgate/schemas (zod 4) is the API contract: import schemas and their z.infer types from '@adgate/schemas' (named exports, one module per responsibility under packages/schemas/src). CONTRACT_SCHEMAS lists every schema that has a JSON Schema file in packages/schemas/json/<Name>.schema.json; regenerate with `pnpm --filter @adgate/schemas gen:json` (json-schema.test.ts fails when a file is stale, and the directory is prettier-ignored). SENSITIVE_TAXONOMY and CATEGORIES_TAXONOMY live in packages/schemas/src/taxonomy.ts and must equal fixtures/classify-fixtures.json (taxonomy.test.ts enforces it); Classification.categories and .sensitive are validated against them. tsx is a root devDependency for running TypeScript dev scripts (`tsx src/foo.ts`) without a build. toJsonSchema (the node:fs writer) and JSON_SCHEMA_DIR live in packages/schemas/src/json-schema-files.ts, deliberately off the package index (index-purity.test.ts pins the index import graph to zod + yaml, no node built-ins); gen-json.ts is the script around it. ClassifyFixture, ClassifyFixtureCase and ClassifyFixtureExpect (packages/schemas/src/classify-fixture.ts, not in CONTRACT_SCHEMAS) are the one schema every test parses fixtures/classify-fixtures.json with; expect.sensitive and categories_any are taxonomy-typed. EvaluateRequest accepts an empty messages array when context_summary is present (refinement: non-empty messages OR non-empty context_summary; JSON anyOf carries minItems 1). Sha256Hash is /^sha256:[0-9a-f]{64}$/ (REVIEW-2; SHA256_HASH_PATTERN exported), so every hash field (PrevHash, record_hash, content_hash, policy_hash, model_output_hash, conversation_id_hash, user_hash, AttestRequest.model_output_hash) needs a real-format value in fixtures: schemas tests use EXAMPLE_SHA256 from packages/schemas/src/doc-examples.fixture.ts, core tests sha256Prefixed('anything'). The JSON examples of docs/api.md and docs/audit.md are read at test time by doc-examples.fixture.ts (docJsonBlocks / docJsonExamples / expectDocExamples keyed by the `## ` heading above the block; fillDocPlaceholders replaces only "sha256:...", "sha256:... or null" and "ed25519:base64..."), so audit-record.test.ts and api-examples.test.ts follow a doc edit instead of drifting; the fixture is off the package index (node:fs).
- Policy (docs/policy.md): PolicyConfig, PolicyOverrides (strict deep partial, now the type of EvaluateRequest.policy_overrides), OverrideRejection and the leaf schemas live in packages/schemas/src/policy*.ts; parsePolicy(unknown) and parsePolicyYaml(text) throw PolicyValidationError (config-time only, never on the request path). @adgate/core owns the pure helpers: canonicalize (packages/core/src/canonical) + sha256Hex/sha256Prefixed (packages/core/src/audit/crypto.ts since S13), policyHash, loadPolicyFromYaml -> { policy, policy_hash }, mergeOverrides -> { policy, rejected } (packages/core/src/policy). packages/core depends on @adgate/schemas (workspace:*) and resolves it from packages/schemas/dist, so turbo builds schemas first; after editing schemas run `pnpm build` before running core tests directly with vitest. fixtures/canonical-json.json holds the canonical JSON vectors and hash.test.ts pins the default policy hash; both are cross-language vectors for the Python SDK. mergeOverrides compares regions.allow on expanded countries (EU = the 27 member states): an override entry is kept when the stored list already covers every country it names (DE on a stored EU), rejected when it would add a country, and an override rejected in full leaves the stored list untouched (an explicit [] still applies: serve nowhere); serve_to_tiers keeps its plain intersection. examples/policy.example.yaml spells out every documented default (koah and gravity disabled included) and equals PolicyConfig.parse({ app_id: 'example-chat' }).
- Policy engine (S05): POLICY_RULES (the seven docs/policy.md rules in order), PolicyRule, PolicyDecisionResult, PolicyDecision {rule, result, detail?} and CapState {session_count, day_count, turns_since_last | null} are Zod schemas in packages/schemas/src/policy-decision.ts and registered in CONTRACT_SCHEMAS (S14's AuditRecord.policy_decisions must be z.array(PolicyDecision)). evaluatePolicy({ classification, user, policy, capState, surface }) -> { allowed, reason, decisions } lives in packages/core/src/policy/evaluate.ts (pure; the gateway reads capState from cap_state and passes it in; turns_since_last is null until the first ad in a conversation). EU expansion: packages/core/src/policy/eu-members.ts (EU_MEMBER_STATES) and regions.ts (expandRegions, isRegionAllowed). The engine does NOT read policy.sensitive_detection (the classifier applies it when merging rule and LLM sensitive signals) and always records competitor_exclusions as pending (mediation resolves it against advertiser_domain). evaluate-purity.test.ts fails if anything under packages/core/src/policy imports node:*, the gateway or an I/O library.
- Rules classifier (S06): packages/core/src/classify/rules. classifyByRules(text) -> RulesResult = Classification (method 'rules', prompt_version RULES_VERSION) plus `matches` (dictionary terms only, never message text); strip matches before persisting (`const { matches, ...classification } = result`). Keyword data is one TS module per category under data/sensitive/<category>.ts and data/commercial/<category>.ts registered in data/index.ts (Record types make a missing taxonomy category a compile error); every phrase is written in normalized form (data.test.ts enforces normalizeText(p) === p: lowercase, no punctuation, contractions expanded to 'do not' / 'can not', alias targets such as 'bitcoin' not 'crypto'); aliases in data/aliases.ts, intent phrases in data/intent.ts, informational phrases in data/informational.ts, scoring constants in data/scoring.ts. RULES_VERSION = sha256:<hex> over canonicalize(RULES_DATA), so any data edit changes it. Fixture contract (fixtures.test.ts): every s* case is caught by rules alone, no c*/l* case gets a sensitive flag, and c001 plus every software.devtools.* case is servable by rules alone (confidence >= 0.7, intent in bounds, listed category). S08 calls classifyByRules on prepareText(input).rulesText (message content only, no role prefixes, last 4 user/assistant messages / 4,000 chars) and keeps every flagged rule category when merging with the LLM's. SensitiveRuleSet.exclusions (optional) lists normalized compounds that maskPhrases (match.ts) blanks out before that one category is matched: weapons excludes glue gun, nail gun, heat gun, staple gun, spray gun, water gun, paint gun and similar tools and toys so 'gun' can stay strong, and a bare 'gun' elsewhere in the same text still matches. apr (finance), dental (health) and naked (adult) are weak terms; rules/false-positives.test.ts pins all of this and data.test.ts checks every exclusion contains a term. packages/core/src/classify/rules/data/ is prettier-ignored (hand-formatted lists).
- LLM classifier (S07): packages/core/src/classify/llm. LlmClassifier { classify(text, { signal? }) -> Promise<LlmClassifyResult> } where LlmClassifyResult is { ok: true, classification (method 'llm', prompt_version PROMPT_VERSION), latency_ms } | { ok: false, reason: 'timeout' | 'parse' | 'invalid' | 'http' | 'network' | 'aborted', latency_ms, detail? } and never throws. OpenAiCompatibleClassifier({ baseUrl, apiKey, model, timeoutMs = 400, fetch }) (fetch is REQUIRED: the gateway passes globalThis.fetch, core never falls back to a platform fetch, and llm/purity.test.ts fails on any globalThis.fetch or fetch( under llm/) POSTs <baseUrl>/chat/completions (temperature 0, response_format json_object, system CLASSIFIER_PROMPT + user text) and races a hard timeout. CLASSIFIER_PROMPT is built from the taxonomy consts; PROMPT_VERSION = sha256:<hex> of the prompt text (computePromptVersion). LlmOutput (the raw model JSON: commercial_intent, categories: string[], sensitive: string[], confidence) lives in @adgate/schemas classification.ts, NOT in CONTRACT_SCHEMAS; core clamps numbers to [0,1] then normalizes lists to the taxonomies (normalizeCategories -> ['general'] when empty, normalizeSensitive). Tests use FakeLlmClassifier(script) with script = fixed result | (text) => result | 'timeout' | 'parse' | 'throw' | 'hang' (throw rejects on purpose, hang never settles), fakeLlmSuccess/fakeLlmFailure builders, and fakeLlmFromFixtures(cases) built from fixtures/classify-fixtures.json cases (the test reads the file and parses it with ClassifyFixture from @adgate/schemas; core never touches fs). Every llm test stubs globalThis.fetch to reject, so nothing can reach the network. The gateway must build the config from CLASSIFIER_BASE_URL / CLASSIFIER_API_KEY / CLASSIFIER_MODEL / CLASSIFIER_TIMEOUT_MS (already in .env.example); core never reads env.
- Classifier orchestrator (S08): packages/core/src/classify (classify.ts, prepare.ts, cache.ts, merge.ts, types.ts, index.ts; re-exported from @adgate/core). ClassificationMethod is now z.enum(['llm','rules','cached']) (JSON schemas regenerated). prepareText({ messages?, context_summary? }, { maxMessages: 4, maxChars: 4000 }) -> { text, rulesText }: only user and assistant messages are read (system and tool are dropped before the last-4 selection; when none remain, context_summary is used), `text` is 'role: content' lines (newest last) for the LLM, the cache key and the gateway's hash and storage, `rulesText` is the same messages' bare content for the rules stage (a role token can never match a phrase); both keep the last 4,000 code points (the string is sliced to 2 * maxChars UTF-16 units before Array.from, so a 1 MB message never becomes a 1 MB array); the gateway must call it so the same `text` is hashed, classified and (only with store_raw_text) stored. classify(input, { llm: LlmClassifier | null, cache?, policy: { sensitive_detection, min_confidence }, timeoutMs? (default DEFAULT_LLM_TIMEOUT_MS = 400), now?, signal? }) -> ClassifyOutcome { classification, source: 'cache' | 'rules_short_circuit' | 'merged' | 'rules_fallback', llm_failure?: LlmFailureReason | 'thrown' | 'unavailable' | 'empty_input', error_name? (only with 'thrown': the Error's name, never its message), cache_key, latency_ms }; it never throws or rejects. The orchestrator races deps.llm.classify against its own deadline and returns rules_fallback with llm_failure 'timeout' when it expires (the timer is cleared either way); the empty-input guard tests rulesText, so a blank or system-only message never reaches the LLM. BALANCED MODE (supersedes the S08 entry): strict = every flagged rule category plus every LLM flag; balanced = every flagged rule category (strong and weak-pair) plus the LLM's flags only when the LLM confidence >= policy.min_confidence; self_harm from either stage at any confidence is never dropped in either mode. Cache: ClassifyCache { get(key), set(key, value) } (implementations must not throw and should validate rows on read; a throw degrades that request to rules-only), createLruCache({ maxEntries = 50_000, ttlMs = 600_000, now }) is the in-memory LRU; classifyCacheKey(text, policy) = sha256:<hex> of normalizeText(text) + PROMPT_VERSION + RULES_VERSION + sensitive_detection + min_confidence joined by newlines. Only short-circuit and merged results are cached; rules-only fallbacks never are. failClosedClassification() is the zeroed rules classification the gateway should use on its own error path. ClassifyOutcome.keywords (S18): keywordsFromRulesMatches over the rules matches (dictionary terms only, never message text), present on EVERY path including a cache hit because the rules stage now runs before the cache lookup (cheap and deterministic; the cache still saves the LLM call), [] when the input could not be prepared; the gateway passes it as DemandRequest.keywords so a warm cache ranks creatives exactly like a cold one.
- Demand adapters (S09): packages/schemas/src/demand.ts holds the catalog and adapter shapes (TargetCategory regex with trailing wildcard, CatalogCreative, SeedCreative = CatalogCreative without id, DemandUser = User.pick(region, locale), DemandRequest { app_id, classification, surface, user, exclusions, keywords? }, Candidate = CatalogCreative + { ecpm_estimate, targeting_match, resolved_url? }, DemandResponse { source, candidates, latency_ms, error? }), all in CONTRACT_SCHEMAS. packages/core/src/demand: DemandAdapter { source; fetch(req, { timeoutMs, signal? }) } never rejects; DEFAULT_DEMAND_TIMEOUT_MS = 250; createDirectAdapter(catalog, { now? }) filters active direct creatives by region (empty target_regions = everywhere, EU token expands, missing region fails closed for targeted creatives), requires a category match (exact 1, trailing wildcard 0.8), adds keyword overlap (targeting_match = 0.75 * category + 0.25 * keyword), orders by compareByRevenue (select.ts: ecpm * targeting_match desc, ecpm desc, id asc, the SAME order mediation ranks with, REVIEW-2; a wildcard match at a high ecpm outranks an exact match at a low one) and returns top 3 with ecpm_estimate = ecpm and resolved_url = url_template; competitor exclusions are NOT applied by adapters (mediation, S11). DemandRequest.keywords must be dictionary terms only: build them with keywordsFromRulesMatches(rulesResult.matches), never from message text. ULIDs: packages/core/src/ids/ulid.ts (ulid, prefixedUlid, isUlid, ulidTime, injectable now/random); assignCreativeIds(seeds, ulidOptions?) turns SeedCreative[] into CatalogCreative[]. demand/purity.test.ts pins the import graph to @adgate/schemas + node:crypto.
- Affiliate adapter (S10): packages/schemas/src/affiliate.ts holds the app-level config the caller supplies: AffiliateConfig { partnerstack?: { program_id }, impact?: { program_id, campaign_id? }, amazon?: { tag, marketplace? (AmazonMarketplace enum over AMAZON_MARKETPLACES) } }, every level z.strictObject; AffiliateConfig and AffiliateNetwork are in CONTRACT_SCHEMAS (41 JSON files; AffiliateConfig is the third schema json-schema.test.ts allows additionalProperties:false for). It carries the app owner's PUBLIC ids only, never credentials or payout details (docs/decisions.md item 6). CatalogCreative.network absent = the creative belongs to whichever network the policy's affiliate demand entry names (the seed file's entry); CatalogCreative.program_id overrides the config program id per creative for partnerstack/impact ONLY (REVIEW-2: it is ignored for amazon, the owner's tag is the only tag a link may ever credit) and never stands in for a missing config entry. packages/core/src/demand/affiliate: template.ts fillTemplate(template, values) fills {{program_id}}, {{campaign_id}}, {{tag}}, {{u}}/{{destination}} (spaces inside the braces ok) with encodeURIComponent(value) wherever they sit (a whole-URL placeholder is unsupported by design: put destinations in a query parameter), failing with missing_placeholder:<name>, unknown_placeholder:<name>, invalid_url (the result must be an absolute http(s) URL) or encode_failed, never throwing; buildPartnerStackUrl / buildImpactUrl / buildAmazonUrl({ template, config, destination? }) -> { ok: true, url } | { ok: false, error }; amazon requires the host to be amazon.<storefront> or www.amazon.<storefront> (amzn.to, smile. and lookalike hosts -> not_amazon_host; a storefront other than config.marketplace -> marketplace_mismatch) and always sets tag=<owner tag> as the last query parameter (setTagParam drops every spelling of an existing tag parameter, TAG= / Tag= / %74ag= included, comparing decoded lower-cased names (REVIEW-2), keeps the rest of the query verbatim and the fragment at the end); registry.ts AFFILIATE_BUILDERS (satisfies Record<AffiliateNetwork, builder>, so a new enum member is a compile error), buildAffiliateUrl({ network, template, config, destination?, program_id? }), isAffiliateConfigured, AFFILIATE_NOT_CONFIGURED = 'affiliate_not_configured'; adapter.ts createAffiliateAdapter({ catalog, network, config, now? }) is a DemandAdapter with source 'affiliate', ONE INSTANCE PER affiliate demand entry (the gateway passes entry.network plus the app's AffiliateConfig and may hand it the whole creatives table): active affiliate creatives of its network, select.ts matchScore + rankCandidates (top 3, byte-identical order to DirectAdapter), {{u}} filled with https://<advertiser_domain>/ (the catalog has no destination field), resolved_url = the built URL; no config entry for the network -> { candidates: [], latency_ms, error: 'affiliate_not_configured' }; an unbuildable creative is skipped and named in error as build_failed:<id>:<reason> (comma-joined, cut at 200 chars) while the ones that built are still returned, so S11 mediation must use candidates even when error is set. Shared helpers factored out of DirectAdapter: select.ts (MAX_CANDIDATES = 3, compareIds, mediationScore, compareByRevenue, rankCandidates in revenue order, matchScore = active + region + targetingScore; compareCandidates is gone, REVIEW-2) and response.ts (Clock, safeNow, latencySince, describeError = the thrown Error's name only or 'NonError', never a message because mediate copies it into the signed trace (REVIEW-2), demandResponse); DIRECT_MAX_CANDIDATES and AFFILIATE_MAX_CANDIDATES alias MAX_CANDIDATES. demand/purity.test.ts walks affiliate/ and pins the implementation file list (add new files there). The gateway must load AffiliateConfig per app (a later story decides the table or env var) and validate it with AffiliateConfig.parse at config time, never on the request path.
- Mediation (S11): DemandTrace (the docs/audit.md `demand` block), DemandResponseSummary (DemandResponse with candidates as a count) and ExcludedCandidate { source, creative_id, advertiser_domain, reason: 'competitor_exclusion' } live in packages/schemas/src/demand-trace.ts and are in CONTRACT_SCHEMAS (44 JSON files); S14's AuditRecord.demand must be DemandTrace. mediate(adapters, req, policy, { timeoutMs = DEFAULT_DEMAND_TIMEOUT_MS, now?, signal? }) in packages/core/src/demand/mediate.ts -> { selected: Candidate | null, selected_source: DemandSource | null, trace: DemandTrace } and never throws or rejects; policy is typed Pick<PolicyConfig, 'competitor_exclusions'>. The gateway passes ONLY the enabled adapters, in policy demand order (mediate does not read policy.demand; it reads policy.competitor_exclusions and req.exclusions); trace.requested is their sources in that order (a source may repeat: one affiliate adapter per network) and responses has one entry each, in the same order. Per-adapter setTimeout + AbortController raced against fetch: at timeoutMs the adapter's signal is aborted and its entry is { candidates: 0, latency_ms: timeoutMs, error: 'timeout' }, a later answer is ignored; a throw, a rejection or an answer without a candidates array is 'adapter_error:<Error.name>' (NonError / InvalidResponse), never a message; the last-resort catch (garbage adapters, request or policy) still writes one { candidates: 0, latency_ms: 0, error: 'adapter_error:<name>' } entry per requested source (REVIEW-2); an adapter's own error string is copied (cut at 200 chars, dropped when empty) and its candidates still compete. latency_ms in the trace is the mediator's clock from dispatch to settle (options.now, default Date.now), not the adapter's self-report. Exclusions (exclusions.ts: normalizeDomain, domainMatches, isExcludedDomain; normalizeDomain strips scheme, userinfo, port, path/query/fragment, a leading `*.` or `.` and trailing dots, REVIEW-2, so *.competitor.com, .competitor.com and https://competitor.com/ all mean competitor.com) match case-insensitively, exact or subdomain (shop.competitor.com by competitor.com; never notcompetitor.com or competitor.com.example); dropped candidates go to trace.excluded in requested-then-candidate order and never rank, while responses[].candidates stays the count the adapter answered with. Ranking: compareByRevenue = ecpm_estimate * targeting_match desc, ecpm_estimate desc, id asc across all sources (mediationScore, compareByRevenue and compareIds live in select.ts since REVIEW-2 and the adapters rank with the same comparator); trace.selected = the winner's adapter source or null (the gateway then records reason no_fill). An already-aborted caller signal calls no adapter (every entry error 'aborted', latency 0); an abort mid-flight settles pending adapters at once with 'aborted'. Exported constants: MEDIATION_TIMEOUT_ERROR = 'timeout', MEDIATION_ABORTED_ERROR = 'aborted', ADAPTER_ERROR_PREFIX = 'adapter_error:'. Tests use demand/mediate.fixture.ts FakeAdapter (scripts respond/hang/throw/reject/malformed, records calls and lastOptions) under fake timers with an afterEach asserting vi.getTimerCount() === 0.
- Partner stubs (S12): KoahConfig and GravityConfig live in packages/schemas/src/network-adapters.ts (one strict-object factory: { enabled: boolean default false, api_key?: string min 1, base_url?: string min 1 }; both in CONTRACT_SCHEMAS, 46 JSON files now, and both in json-schema.test.ts's additionalProperties:false list; NetworkAdapterConfig = KoahConfig | GravityConfig). They are config-time input like AffiliateConfig, but api_key IS a secret: never in a DemandRequest/DemandResponse, the audit record or a log. packages/core/src/demand/network-stub.ts holds the shared NetworkStubAdapter<Source>(source, config, { now? }) plus NETWORK_NOT_CONFIGURED = 'not_configured', NETWORK_NOT_IMPLEMENTED = 'not_implemented' and isNetworkConfigured(config); koah.ts / gravity.ts export KoahAdapter / GravityAdapter and createKoahAdapter(config, options?) / createGravityAdapter(config, options?) (sources 'koah' / 'gravity'), each with a header naming docs/decisions.md item 10 and the story-allowed TODO block (expected request and response shape, `Partner docs: TBD`). Behaviour, in order: aborted signal -> { candidates: [], error: 'aborted' }; enabled !== true or a missing/blank api_key or base_url -> error 'not_configured'; enabled with both -> error 'not_implemented' (docs/decisions.md item 10: no guessed request shape, no network call). The adapter keeps only a boolean from its config, never the credential. purity.test.ts pins koah.ts and gravity.ts to contain no `fetch(` or globalThis at all and the three files' import graph to @adgate/schemas alone. Not yet wired: the gateway story should parse KOAH_*/GRAVITY_* env vars (not in .env.example yet) with KoahConfig.parse/GravityConfig.parse at boot and hand the stubs to mediate() only when the policy demand entry is enabled.
- Audit crypto (S13): packages/core/src/audit is the single home of hashing and signing (canonical/sha256.ts is gone; import sha256Hex/sha256Prefixed from '../audit/crypto.js' or @adgate/core; both accept string | Uint8Array). SIGNING RULE: sign(recordHash, privatePem) signs the UTF-8 bytes of the record_hash STRING exactly as it appears in the record, `sha256:` prefix included, and returns `ed25519:` + standard padded base64 of the 64 raw bytes (always 96 characters); verifySignature(hashString, signature, publicPem) and ring.verify never throw (wrong prefix, bad base64/padding/length, another key, a non-Ed25519 key, a garbage PEM or non-string inputs all give false). Keys: generateKeypair({ now?, keyId? }) -> { key_id (default k_<YYYY>_<MM> in UTC from the clock, e.g. k_2026_09), private_pem (PKCS#8), public_pem (SPKI) }; derivePublicPem(privatePem); loadPrivateKey/loadPublicKey return a node KeyObject and check the PEM header first, so a private PEM is never accepted as a public key; createKeyRing(Record<key_id, publicPem>) -> PublicKeyRing { key_ids, has, verify(hash, signature, key_id) -> { ok: true, detail: 'key_id=<id>' } | { ok: false, detail: 'unknown_key_id' | 'bad_signature' } } loads every PEM once at construction (S15 reports that detail on the signature check); parsePublicKeysJson(text) parses ADGATE_PUBLIC_KEYS_JSON (PEM line breaks are \n escapes inside the JSON string; OpenSSL rejects one-line PEMs). Config-time failures throw AuditKeyError { reason: 'invalid_json' | 'invalid_shape' | 'invalid_key_id' | 'invalid_public_key' | 'invalid_private_key', key_id? } whose messages never carry key material; sign also throws it for an unusable key, so the gateway must load the signing key (loadPrivateKey) and the ring (parsePublicKeysJson + createKeyRing) at boot, never on the request path, and must add its own signing key's public half (derivePublicPem) under ADGATE_SIGNING_KEY_ID when the JSON lacks it. Schemas: KeyId (KEY_ID_PATTERN /^[A-Za-z0-9][A-Za-z0-9_.-]{0,63}$/), Ed25519Signature (ED25519_SIGNATURE_PATTERN), PublicKeyPem (PUBLIC_KEY_PEM_PATTERN) and PublicKeysJson = z.record(KeyId, PublicKeyPem) in packages/schemas/src/keys.ts, all four in CONTRACT_SCHEMAS (50 JSON files; json-schema.test.ts has a `records` list for PublicKeysJson because z.record renders additionalProperties as the value schema). S14's AuditRecord should type record_hash with Sha256Hash, prev_hash as Sha256Hash or the literal 'genesis', signature as Ed25519Signature and key_id as KeyId. audit/purity.test.ts pins the file list (crypto.ts, errors.ts, index.ts, keys.ts) and the import graph to @adgate/schemas + node:crypto; shared test material is in audit/crypto.fixture.ts (TEST_KEYS, OTHER_KEYS, EC_KEYS, RECORD_HASH, flipLastHexDigit, tamperSignature, catchAuditKeyError).
- Audit record (S14): packages/schemas/src/audit-record.ts holds AuditRecord (docs/audit.md, fields declared in doc order with key_id last, plus `override_rejected: OverrideRejection[]` right after policy_decisions, defaulting to [] because docs/policy.md says loosening overrides are noted in the record as override_rejected while the docs/audit.md example omits it), UnsignedAuditRecord (= AuditRecord minus record_hash and signature), AuditRecordBody (= minus prev_hash, key_id, record_hash, signature), PrevHash (Sha256Hash | literal 'genesis'; GENESIS_PREV_HASH), AuditSurface (Surface without max_creatives), AuditCreative { id, advertiser, advertiser_domain, content_hash } and AuditDisclosure (label/position/style with NO defaults: a signed record is never back-filled). AuditRecord alone is in CONTRACT_SCHEMAS (51 JSON files) and carries the same serve/suppress refinements and allOf meta as EvaluateResponse (serve needs a creative and a null reason, suppress a null creative and a reason); the derived schemas are cut from the unrefined shape. packages/core/src/audit: privacy-hash.ts (conversationIdHash(salt, id) = sha256Prefixed(salt + id); userHash(salt, value) -> null when undefined/null/'', a 64-hex digest (sha256: prefix optional, any case) normalised to sha256:<lowercase hex> and NEVER re-hashed so per-user caps key on one value, anything else salted and hashed like a conversation id so a raw id is never stored; normalizeSha256Hash(value); an empty salt or a non-string throws TypeError), content-hash.ts (CREATIVE_CONTENT_FIELDS = advertiser, advertiser_domain, headline, body, cta, url_template; creativeContent(c) picks exactly those; creativeContentHash(c) = sha256Prefixed(canonicalize(creativeContent(c))) and accepts a Candidate or a creatives row: S20's creative_hash check must recompute it over the stored row with this function; id, ecpm, ecpm_estimate, targeting_match, resolved_url, targeting lists, keywords, active, source, network and program_id are excluded on purpose), chain.ts (GENESIS = 'genesis'; AuditSigningKey { key_id, private_pem }; unsignedOf(record) removes record_hash and signature and nothing else, so the record is hashed AS GIVEN and a field added after signing changes the hash; recordHashInput(record) = canonicalize(unsignedOf(record)) + record.prev_hash and computeRecordHash = sha256Prefixed of it, literally docs/audit.md (prev_hash sits inside the canonical JSON and is appended once more); signRecord(record, signing) sets key_id from signing.key_id, discards any stale record_hash/signature, hashes and signs with S13's sign(), throwing AuditKeyError for a malformed key_id or an unusable PEM; nextPrevHash(previous | null | undefined)), record.ts (BuildAuditRecordInput { id (prefixedUlid('aud_')), app: { app_id, salt }, conversation_id, user_hash?, turn_id, ts (IsoTimestamp, injected), surface, classification, policy: { policy_version, policy_hash, disclosure }, policyResult (PolicyEvaluation is assignable), override_rejected? (mergeOverrides().rejected), mediation: { selected, trace } | null (MediationResult is assignable; null = demand never ran), prev_hash, signing }; buildAuditBody(input) -> AuditRecordBody; buildAuditRecord(input) -> signed AuditRecord; emptyDemandTrace()). Decision rules: policy failed -> suppress with policyResult.reason (null -> 'error'), creative null, and the trace is still copied if a mediation was handed in; policy passed and mediation.selected -> serve with creative { id, advertiser, advertiser_domain, content_hash }; otherwise suppress no_fill (mediation null gives { requested: [], responses: [], excluded: [], selected: null }). Every value is picked field by field (the six Classification fields, surface without max_creatives, decisions with detail only when present, rejections) and the trace is structuredClone'd, so the record shares nothing with the caller and a RulesResult's matches never reach it. New records always carry model_output_hash null, separation_attestation false, attested_at null, supersedes_hash null and an explicit override_rejected, so the stored JSON and its parse are identical and hash the same. Gateway obligations (S18): prev_hash = nextPrevHash(the app's latest record by ts/id), id = prefixedUlid('aud_'), key the cap_state user row on the same userHash() output, and on the error path build the record from failClosedClassification() with policyResult { allowed: false, reason: 'error', decisions: [] } and mediation null. S15 built attest and verify on this (see its bullet). audit/purity.test.ts pins the implementation file list (eleven files since S15) and the import graph (@adgate/schemas + node:crypto; ../canonical is relative). Test material: audit/record.fixture.ts (APP, CONVERSATION_ID, RAW_USER_ID, TS, POLICY_HASH, SERVE_CLASSIFICATION, CHAT_SURFACE, DISCLOSURE, SERVE_DECISIONS, PAID_USER_DECISIONS, EXAMPLE_CANDIDATE, SERVE_TRACE, NO_FILL_TRACE, serveInput(patch), suppressInput(reason, decisions?, patch?)).
- Attest and verify (S15): packages/core/src/audit/attest.ts and verify.ts (plus verify-checks.ts, internal: its check functions are not on the package index). attest(record, model_output_hash, now, { prev_hash, signing }) -> a NEW signed AuditRecord with the SAME id, prev_hash = nextPrevHash(the app's latest record at attestation time) (never genesis), supersedes_hash = original.record_hash, attested_at = now, separation_attestation true, key_id from signing (rotation-safe); the copy is built from AuditRecord.parse(record), so unknown keys are dropped, override_rejected is present and nothing is shared with the input; suppress records are attestable (the SDK attests every turn). Throws AuditAttestError { reason: 'invalid_record' | 'already_attested' | 'invalid_model_output_hash' | 'invalid_attested_at' | 'invalid_prev_hash' } (request-time: map to 4xx, already_attested to 409) or AuditKeyError for an unusable signing key. The gateway (S19) must store BOTH rows keyed by record_hash and mark the newest version per id; GET /v1/audit/:id and /v1/verify/:id serve the newest. verify(record: unknown, ring: PublicKeyRing, ctx?) -> VerifyResponse never throws (schema failure = every later check ok:false 'skipped: schema invalid'; a throwing check = ok:false 'error: <Error.name>'); record_hash is recomputed over the RAW object as loaded (an added column value fails it), the other checks read the parsed record. ctx = { prevRecord?: AuditRecord | { pruned: true, retain_cutoff } | null, storedCreativeHash?: string | null, supersededBy?: AuditRecord | null, supersededRecord?: AuditRecord | null, verifySuperseded?: boolean, superseded?: VerifyContext } (REVIEW-2 semantics): prevRecord is the POSITIONAL predecessor, the app's record immediately before this one in chain order (audit_records.seq - 1), NEVER "the record whose record_hash equals prev_hash" (a fork pointing at an older record must fail 'prev_hash does not match the previous record', and a re-signed genesis record in the middle of a chain fails 'genesis record given a previous record'); null for the app's first record; { pruned: true, retain_cutoff: IsoTimestamp } when S37 deleted it -> chain ok 'pruned' only for a non-genesis record with ts >= retain_cutoff, else 'record predates the retention cutoff of its pruned predecessor'; storedCreativeHash = creativeContentHash(the creatives row of record.creative.id); supersededBy = the attesting record when verifying an older version of an id directly (S20), which makes an unattested record's separation_attested ok 'attested by superseding record'; supersededRecord by record_hash === record.supersedes_hash together with ITS OWN positional prevRecord under ctx.superseded (required unless the original is genesis; ctx.superseded.storedCreativeHash is inherited from the outer context when undefined, and supersededBy is set automatically for the nested verification). The supersedes check also requires the attestation to be a faithful copy of the superseded record outside ATTESTATION_FIELDS (model_output_hash, separation_attestation, attested_at, supersedes_hash, prev_hash, key_id, record_hash, signature; verify-supersedes.ts, detail 'body_mismatch', runs even with verifySuperseded false), and the superseded record must not itself be an attestation. Details are the VERIFY_DETAIL constants exported from @adgate/core; ok checks carry no detail except signature 'key_id=<id>', chain 'genesis'/'pruned', 'not applicable' (creative_hash on suppress, separation_attested on an unattested suppress record for S20, supersedes when null) and the supersedes verdict. An unattested serve record is valid:false with separation_attested 'not attested' (docs: true only if attested); a record whose attestation is handed in as ctx.supersededBy (or reached through supersedes_hash, where verify sets it) reports separation_attested ok 'attested by superseding record' when unattested. VERIFY_CHECK_ORDER = VerifyCheckName.options. describeIssues (zod issues -> bullet lines, never values) now lives in audit/errors.ts and is shared by keys.ts and attest.ts.
- Gateway skeleton (S16): packages/gateway is a Hono app. config.ts: loadConfig(env = process.env) parses one Zod schema (GatewayEnv, blank values count as unset) into GatewayConfig { nodeEnv, port (8787), logLevel, databaseUrl, databaseUrlTest | null, corsAllowedOrigins: string[], publicBaseUrl, metricsToken | null, adminPassword | null, signing: { keyId, privatePem, publicKeysJson }, classifier: { baseUrl, apiKey ('' = no header), model, timeoutMs } | null (null = rules only, when CLASSIFIER_MODEL or CLASSIFIER_BASE_URL is unset; shaped for OpenAiCompatibleClassifier plus fetch), classifierTimeoutMs (400), koah / gravity (KoahConfig / GravityConfig from KOAH_* / GRAVITY_*) } and throws ConfigError { issues: string[] } listing every missing/invalid variable by name with a hint (an empty ADGATE_SIGNING_KEY_PEM says to run `pnpm --filter @adgate/gateway keygen`); literal \n in the PEM is unescaped. signing.ts: loadSigningKeys(config.signing) -> { signing: AuditSigningKey, ring: PublicKeyRing } (loadPrivateKey + parsePublicKeysJson + createKeyRing; the signing key's public half is added under its key_id, a mismatching entry is a ConfigError; never on the request path). logger.ts: createLogger({ level }, destination?) is pino with base { service: 'adgate-gateway' }, ISO time, and formatters.log = redactSensitive, which replaces the value of any key named messages, context_summary, authorization, cookie, api_key, password, private_pem, hashed_key (case-insensitive, ANY depth) with '[Redacted]'; still never pass bodies to a logger. request-id.ts: requestContext(logger) middleware sets c.get('requestId') / c.get('logger') (child with req_id), echoes/mints X-Request-Id (REQUEST_ID_PATTERN, else a ULID) and writes one 'request' line { method, path, status, duration_ms } per request; app-env.ts AppEnv = { Variables: { requestId, logger } } for `new Hono<AppEnv>()`. app.ts: createApp({ logger, corsAllowedOrigins, db? }) -> Hono<AppEnv> with cors (allowHeaders Authorization/Content-Type/X-Request-Id), GET /healthz, notFound and onError in the docs/api.md error shape via errorResponse(c, status, code, message, headers?) and errorCode(status) (401 unauthorized, 404 not_found, 409 conflict, 429 rate_limited, 500 internal_error, else http_<n>); throw HTTPException(status, { message, res? }) from routes/middleware for 4xx (res headers such as Retry-After are copied), anything else becomes 500 internal_error and is logged with err + path (the /v1/evaluate 200-on-error rule is that route's own job). server.ts (entry): loadRootEnvFile() (env-file.ts: Node's process.loadEnvFile on <repo root>/.env, found via pnpm-workspace.yaml; existing env vars win; no dotenv) -> loadConfig -> loadSigningKeys (ConfigError prints and exits 1 before a port opens) -> createDb -> createApp -> @hono/node-server serve, SIGINT/SIGTERM close server + pool. cli.ts: isMainModule(import.meta.url), redactDatabaseUrl. Scripts: dev (tsx watch src/server.ts), start (node dist/server.js), db:migrate (tsx src/db/migrate.ts [url], else DATABASE_URL), db:generate (drizzle-kit generate; pass --name <x>), keygen (prints ADGATE_SIGNING_KEY_ID / ADGATE_SIGNING_KEY_PEM (double-quoted, \n escapes, which Node's .env parser expands) / ADGATE_PUBLIC_KEYS_JSON lines; writes nothing). Build: tsup entries index, server, db/migrate, keygen (dist keeps the db/ directory); keygen.ts and db/migrate.ts are NOT re-exported from index.ts on purpose (their main-module guard needs their own import.meta.url). Dependencies: hono, @hono/node-server, drizzle-orm, postgres (postgres.js), pino, zod; dev drizzle-kit. No supertest (tests use app.request()), no dotenv, no pino-pretty.
- Gateway database (S16): Drizzle tables in packages/gateway/src/db/tables/{apps,catalog,audit,caps}.ts (+ columns.ts helpers timestamptz/createdAt/updatedAt/sqlList), barrel src/db/schema.ts with ALL_TABLES and TABLE_NAMES; client.ts createDb(url, { max? }) -> DbHandle { db: Db (PostgresJsDatabase<typeof schema>), sql (postgres.js), close() }; migrate.ts runMigrations(url, folder = findMigrationsDir()) applies packages/gateway/drizzle (0000_init.sql + meta/, committed; drizzle tracks applied ones, so re-runs are no-ops). Column naming is explicit snake_case with camelCase TS keys (apps.policyYaml <-> policy_yaml); timestamptz columns are Date objects (mode 'date'): call toISOString() at the API boundary; numeric ecpm is mode 'number'; text[] for lists; jsonb columns are $type'd (AuditRecord, Classification, AffiliateConfig). RULE: table modules import @adgate/schemas as TYPES only (drizzle-kit loads them through a CJS hook that cannot resolve the ESM-only workspace packages: ERR_PACKAGE_PATH_NOT_EXPORTED), so CHECK lists are local tuples AUDIT_DECISIONS / EVENT_TYPES / CREATIVE_SOURCES that schema.test.ts pins to Decision.options / EventType.options / DemandSource.options; after any table edit run `pnpm --filter @adgate/gateway db:generate --name <change>` (no DB needed) and commit the SQL + meta. Tables: apps (id app_, name, salt, policy_yaml, policy_hash, policy_version int default 1, affiliate_config jsonb null, created_at, updated_at); advertisers (id adv_, name, domain unique); api_keys (id key_, app_id fk, key_prefix unique = the public lookup handle of a presented key ak_<prefix>_<secret>, hashed_key (argon2 of the secret, S17), role app | advertiser_read (CHECK) with advertiser_id required exactly for advertiser_read (CHECK), created_at, revoked_at null, last_used_at null); creatives (id cr_, advertiser_id fk, app_id fk null = global catalog, headline, body default '', cta, url_template, target_categories/target_regions/keywords text[], ecpm numeric(12,4) >= 0, source CHECK direct|affiliate|koah|gravity, network null, program_id null, active default true, content_hash = creativeContentHash over the row joined with advertiser name/domain, created_at, updated_at; indexes advertiser_id, app_id); audit_records (pk record_hash; id aud_ NOT unique because attestation inserts a second row with the same id; app_id fk, prev_hash, supersedes_hash null, is_latest default true, seq integer NOT NULL (drizzle/0001_audit_seq.sql, S18: the 1-based per-app chain position, UNIQUE (app_id, seq) as audit_records_app_id_seq_uidx; every insert, attested rows included, must take the app's next seq under the apps-row lock of evaluate/audit-store.ts, verify's prevRecord is the row at seq - 1 of the same app (null at seq 1, { pruned: true, retain_cutoff } when S37 deleted it), and the latest record is the highest seq), decision CHECK serve|suppress, reason null, creative_id null, advertiser_id null, ts, record jsonb = the signed AuditRecord, created_at; indexes (app_id, ts), (id), (app_id, is_latest), (advertiser_id, ts), partial UNIQUE (id) WHERE is_latest = true, so S19 must flip the old row's is_latest to false before inserting the attested one); events (id ev_, audit_id (no FK), app_id fk, type CHECK impression|click|dismiss|conversion, ts, meta jsonb default {}, created_at; index (audit_id), (app_id, ts), partial UNIQUE (audit_id) WHERE type = 'impression': a duplicate impression insert violates it, use ON CONFLICT DO NOTHING); cap_state (pk (app_id, conversation_hash) named cap_state_pkey; user_hash null, session_count default 0 = CapState.session_count, turn_count default 0 = evaluate calls seen in the conversation (increment every call), last_turn_index null = the turn_count at the last served ad, so turns_since_last = turn_count - last_turn_index or null; updated_at); user_day_caps (pk (app_id, user_hash, day date YYYY-MM-DD UTC), count = CapState.day_count across conversations, updated_at); classify_cache (hash pk = classifyCacheKey, classification jsonb, expires_at; index expires_at); raw_text (audit_id pk, app_id fk, text, created_at; written only when policy.privacy.store_raw_text). FKs are plain (no cascade): deleting an app with data fails loudly. Tests: the gateway vitest runs files serially (fileParallelism: false, testTimeout 20 s) and vitest.setup.ts loads the repo-root .env; integration test files call requireTestDatabaseUrl() (src/db/test-support.ts: clear error when DATABASE_URL_TEST is unset, and the database name must contain "test"), runMigrations(url) then truncateAllTables(handle.sql) in beforeAll, createDb(url, { max: 2 }) and handle.close() in afterAll; src/test-support/logs.ts collectLogs() gives an in-memory pino destination. `pnpm db:migrate` verified against the docker Postgres (10 tables, the four required indexes) and both boot paths verified with a generated key (healthz, X-Request-Id echo, 404 body, CORS, clean SIGTERM shutdown).
- Auth and seeding (S17): API keys are `ak_<prefix>_<secret>` (packages/gateway/src/auth/keys.ts: API_KEY_PATTERN /^ak_([A-Za-z0-9]{8,12})_([A-Za-z0-9_-]{32,128})$/; the prefix is 12 uniform alphanumerics with no `_`, so the first `_` after `ak_` always ends it, stored in clear in api_keys.key_prefix; the secret is 32 random bytes as base64url (43 chars) that exists only in the caller's hands; hashed_key = argon2id of the secret with ARGON2_OPTIONS m=19456 KiB, t=2, p=1, the OWASP minimum, because the secret already carries 256 random bits; argon2 writes the PHC parameters alphabetically `m=..,p=..,t=..` and isArgon2idHash accepts any order). Helpers: generateApiKey(random?), parseApiKey, parseBearerHeader (Bearer case-insensitive, one token, nothing else), hashApiSecret, verifyApiSecret (never throws; a corrupt or foreign hash is false), burnVerifyTime(secret) (a decoy verify so an unknown prefix costs the same as a wrong secret). auth/repository.ts: ApiKeyStore { findByPrefix, findApp, touchLastUsed } (createApiKeyStore(db) is the Postgres one; unit tests script it), issueApiKey(db, { appId, role, advertiserId?, ulid? }) -> IssuedApiKey { key_id (key_ ULID), app_id, role, advertiser_id, key_prefix, secret, api_key } (advertiser_read requires advertiserId, app forbids it: TypeError; the table CHECK agrees), revokeApiKey(db, keyId) -> boolean. auth/middleware.ts: bearerAuth({ store, roles, now? }) for routes on `new Hono<AppEnv>()`: parse -> findByPrefix (none: decoy verify) -> verifyApiSecret -> revoked? -> findApp -> role. Every authentication failure (missing/other scheme/malformed/unknown prefix/wrong secret/revoked/missing app/store error) is HTTPException 401 with body { error: { code: 'unauthorized', message: 'missing or invalid API key' } } (UNAUTHORIZED_MESSAGE) and WWW-Authenticate: Bearer realm="adgate"; a key whose role is not in `roles` is 403 forbidden (after authentication, so advertiser_read on evaluate is 403, never 401). On success c.get('auth') = AuthContext { key_id, app_id, role, advertiser_id } (app-env.ts), c.get('app') = the apps row (loaded for BOTH roles; advertiser_read keys also have app_id NOT NULL), c.get('logger') becomes a child with key_id and app_id, and last_used_at is written fire-and-forget at most once per LAST_USED_THROTTLE_MS (60 s) with a warn on failure. Log lines carry key_id only, never the key, secret or hash (the logger's api_key/hashed_key/authorization redaction is the backstop). NOT yet mounted in createApp: S18/S19/S20 add it per route, e.g. `app.post('/v1/evaluate', bearerAuth({ store: createApiKeyStore(db), roles: ['app'] }), handler)`, and evaluate must also check body.app_id === c.get('auth').app_id. apps/register-app.ts: registerApp(db, { name, policyYaml?, ulid? }) -> { app: AppRow, policy, key } (named registerApp because createApp is the Hono factory on the same index) inserts the apps row (app_ ULID, 32-byte hex salt, policy_yaml stored verbatim, or defaultPolicyYaml(id) = one comment line + `version: 1` + `app_id: <id>`, policy_hash from loadPolicyFromYaml, policy_version 1, affiliate_config null) and its first app key in one transaction; PolicyValidationError is thrown before any write; names are not unique. catalog/seed.ts: seedCreatives(db, entries: unknown[], { appId?, ulid? }) -> { advertisers: { inserted, updated, unchanged }, creatives: { same } } parses with SeedCreative.array(), upserts advertisers by domain (adv_ ULIDs; two names for one domain in one file throws) and creatives by (advertiser, headline) WITHIN ONE CATALOG (app_id null = global; --app = that app's private catalog; the two never touch each other), content_hash = creativeContentHash(seed), network/program_id from the seed or null, cr_ ids assigned on first insert only, changed entries updated in place (updated_at bumped), unchanged ones untouched, all in one transaction (an unknown app id or an invalid entry writes nothing). Scripts live in src/scripts/ (src/cli.ts already existed) and are tsup entries: `pnpm --filter @adgate/gateway create-app --name <n> [--policy <yaml>]`, `create-key --app <app_id> --role app|advertiser_read [--advertiser <adv_id>]`, `seed-creatives [--file <json>] [--app <app_id>]` (default <repo root>/examples/creatives.seed.json); each loads the repo-root .env, connects to DATABASE_URL with one connection, prints the key ONCE, and exits 1 with the usage on a UsageError (scripts/args.ts parseFlags/requireFlag/optionalFlag; a bare `--` is ignored because pnpm 9 forwards it to the script). Row types AppRow / ApiKeyRow / AdvertiserRow are in db/tables/apps.ts, DbOrTx (a Db or the transaction handle) in db/client.ts. Dependency: argon2 ^0.45.1 (CommonJS native addon with prebuilds for linux-x64 and darwin-arm64; import it as a default import; pnpm 9 runs its install script without an allowlist).
- Evaluate route (S18): POST /v1/evaluate is mounted by createApp when AppDeps.evaluate is given (an EvaluateDeps from createEvaluateDeps(config, db, overrides?) in packages/gateway/src/evaluate/deps.ts), behind bearerAuth({ roles: ['app'] }); server.ts passes { signing: keys.signing }. HTTP boundary (evaluate/route.ts): a body that is not JSON or not an EvaluateRequest is 400 { error: { code: 'invalid_request', message: '<path: zod message>' } } (paths and types, never values), body.app_id !== auth.app_id is 403 forbidden; everything after that is evaluate(deps, ctx) in evaluate/pipeline.ts, which never rejects: any throw becomes HTTP 200 { decision: 'suppress', reason: 'error', classification: failClosedClassification(), creative: null, audit_id, latency_ms } plus an error-level log line with error_name only and a fail-closed audit record of its own (policyResult { allowed: false, reason: 'error', decisions: [] }, mediation null, policy_hash = apps.policy_hash, disclosure from the policy or the defaults, the turn still counted) written through the same store; when even that fails the response carries a fresh aud_ id and the log says 'audit record not persisted'. Pipeline order: policies.load(app) (evaluate/policy-loader.ts memoises loadPolicyFromYaml per (app id, policy_hash), 1,000 entries) -> mergeOverrides(stored, policy_overrides ?? {}) (rejected[] -> record.override_rejected; record.policy_hash = policyHash(effective policy), equal to apps.policy_hash when nothing was accepted; policy_version = apps.policy_version) -> conversationIdHash/userHash with apps.salt -> caps.read (evaluate/caps.ts: cap_state row + user_day_caps count for (app, user_hash, utcDay(now)) -> CapState { session_count, day_count, turns_since_last = last_turn_index === null ? null : turn_count - last_turn_index }) -> prepareText + classifyCacheKey -> classifyCache.open(key, log) (evaluate/classify-cache-pg.ts: the process LRU first, then the classify_cache table where expires_at > now with rows validated by Classification.safeParse, seeding the LRU; source 'lru' | 'pg' | 'miss'; set writes the LRU and queues a 10 minute upsert; every Postgres error is a warn + miss) -> classify(request, { llm, cache, policy, timeoutMs: config.classifierTimeoutMs, now }) -> evaluatePolicy -> when allowed adapters.build({ app, policy, log }) (evaluate/adapters.ts: active creatives with app_id null or = app joined with advertisers, loaded per request and validated row by row with CatalogCreative.safeParse, bad rows skipped with a warn; one adapter per enabled policy.demand entry in order: DirectAdapter, one AffiliateAdapter per affiliate entry over AffiliateConfig.parse(apps.affiliate_config) or {} which answers affiliate_not_configured, Koah/Gravity stubs from config.koah/config.gravity) and mediate(adapters, { app_id, classification, surface, user: { region?, locale? }, exclusions: [], keywords: outcome.keywords }, policy, { timeoutMs: 250, now }) -> buildAuditBody + buildEvaluateResponse (evaluate/response.ts; validated with EvaluateResponse.parse BEFORE persisting so a contract failure never leaves a serve record) -> auditStore.persist (evaluate/audit-store.ts, ONE transaction: SELECT apps FOR UPDATE, readLatestAudit (highest seq), build(previous) = buildAuditRecord with id prefixedUlid('aud_') and prev_hash = nextPrevHash(previous), insert audit_records with seq = previous + 1, is_latest true, creative_id, advertiser_id; applyCapUpdates (turn_count + 1 always; on serve session_count + 1, last_turn_index = the new turn_count, user_day_caps + 1 when user_hash); raw_text insert of the prepared 'role: content' text ONLY when the effective policy.privacy.store_raw_text is true) -> cache.flush() -> the response derived from the persisted record (creative.url = `${PUBLIC_BASE_URL}/c/${audit_id}`, disclosure_label = effective policy.disclosure.label), parsed once more. One info line 'evaluate' per call: req_id/key_id/app_id (from the bearerAuth child logger), audit_id, decision, reason, latency_ms, classification_method, classify_source, cache_source, llm_failure?, error_name?, persisted, seq; never messages, context_summary or the key. Injection points (EvaluateDepsOverrides): signing, llm (null = rules only, undefined = from config: OpenAiCompatibleClassifier over globalThis.fetch), now, lru, classifyCache, policies, caps, adapters, auditStore, classifierTimeoutMs, demandTimeoutMs. Test harness: src/evaluate/test-support.ts createHarness({ policyYaml?, seed?, overrides?, logLevel? }) -> { post(body, { apiKey?, raw? }), evaluate(body), reset(), close(), deps, keys, registered, appId, apiKey, lines }, evaluateBody(appId, patch), TEXT (c001 serves, s001 sensitive_category:health, l011 low_commercial_intent, l014 low_confidence, all by rules alone), auditRow, auditRowsInOrder, storedCreativeHash, verifyRow, expectChecks(result, failing[]), tablesContaining(handle, needle). An unattested SERVE record verifies with every check ok except separation_attested ('not attested'); suppress records are fully valid. S19 (attest) must append through the same apps-row lock and take the next seq (flip the old row's is_latest to false first); S20 loads prevRecord as the row at seq - 1. src/http-error.ts holds errorCode/errorResponse (re-exported from app.ts) so routes can render 4xx without importing app.ts.

## 2026-09-02 S01 Monorepo scaffold
Built: pnpm workspaces + Turborepo 2, TypeScript 5.9 strict ESM (NodeNext), Vitest 3 (root projects runner + per-package vitest.config.ts), ESLint 10 flat config + typescript-eslint 8 + Prettier 3, tsup builds. Packages @adgate/schemas, @adgate/core, @adgate/gateway, @adgate/sdk each have package.json, tsconfig.json, src/index.ts (exports packageName) and a passing smoke test. Verified from a clean copy with pnpm install --frozen-lockfile.
Decisions: All tooling devDependencies live at the workspace root only; packages declare none (pnpm exposes root bins to workspace scripts). Relative imports inside src use the .js extension (NodeNext); vitest and tsup resolve them. Tests live in src/**/*.test.ts and are typechecked by tsc --noEmit (tsconfig include: src). build = tsup src/index.ts --format esm --dts; the exports map points at dist, and turbo test/typecheck dependsOn ^build so cross-package imports resolve once packages depend on each other. pnpm test/typecheck/lint run per package through turbo; `pnpm exec vitest run` at the root runs everything via projects. ESLint bans default exports inside packages/*/src (CLAUDE.md: named exports only); tool config files are exempt. `pnpm lint` is ESLint only; `pnpm format:check` is Prettier and is a separate gate. prd.json, fixtures/, docs/ and *.md are prettier-ignored on purpose (hand-formatted, contract docs).
Gotchas: ESLint 9 is end of life (pnpm prints deprecated) so the repo is on ESLint 10 + @eslint/js 10. `turbo run dev` with no package dev scripts exits 0 with zero tasks, so no placeholder dev script exists; gateway db:migrate is the only placeholder script (allowed until S16). packageManager must be an exact version (pnpm@9.15.9) for turbo and corepack. Prettier reformatted docker-compose.yml, examples/*.json|yaml and pnpm-workspace.yaml (quotes and wrapping only; no semantic change). For S02: CI should use Node 20 and `pnpm install --frozen-lockfile`; the compose default host port 5432 is correct in CI (5433 is only this dev machine, see Codebase notes).
Ideas: root tsconfig.json for editor support of root config files; coverage thresholds once real logic lands.

## 2026-09-02 S02 Local infra, CI, license, README
Built: .github/workflows/ci.yml (push + pull_request; Node 20; pnpm pinned via packageManager; frozen-lockfile install; postgres:16 service container with a psql step that creates adgate_test; typecheck, lint, test with DATABASE_URL/DATABASE_URL_TEST on 5432). LICENSE fetched verbatim from https://www.apache.org/licenses/LICENSE-2.0.txt. README rewritten around the required headings "What adgate is", "What it is not", "Quickstart" (placeholder until S39), "Repo layout". .env.example now has a comment for every variable including the nine required ones. docker-compose.yml and scripts/db/init.sql were already complete from the starter and are unchanged (host port stays ${PGPORT_HOST:-5432}).
Decisions: pnpm/action-setup gets no explicit version so it reads package.json packageManager (pnpm@9.15.9); bump only that field. adgate_test in CI is created with psql plus a WHERE NOT EXISTS guard and \gexec (idempotent on re-runs) because service containers cannot mount the compose init script. DATABASE_URL and DATABASE_URL_TEST are job-level env so every step, not just test, sees them. Workflow adds permissions: contents: read and a per-ref concurrency group that cancels superseded runs. No dependencies added.
Gotchas: There is no GitHub remote, so the workflow was verified locally, not on GitHub: the YAML was parsed with js-yaml (transitive dep in node_modules/.pnpm) and checked for triggers, node-version 20, service image, ports and job env; the psql step was run verbatim against the docker Postgres on 5433 after dropping adgate_test (created it, second run was a no-op, exit 0); then the exact command sequence pnpm install --frozen-lockfile, pnpm typecheck, pnpm lint, pnpm test ran green with the job env exported, and pnpm format:check passed. psql -c cannot mix SQL with \gexec, so the step feeds psql through a heredoc. zsh treats `status` as a read-only variable; do not use it as a shell variable name in scripts. Prettier formats .github/workflows/*.yml (singleQuote), so run pnpm format:check after editing the workflow. pnpm install --frozen-lockfile added four empty `packages/<name>: {}` importer entries to pnpm-lock.yaml that S01 had not committed (packages have no deps yet); committed here so CI's frozen install is stable, and a second frozen install leaves the lockfile untouched.
Ideas: add pnpm format:check as a CI step once the loop stabilises; cache turbo outputs in CI with actions/cache keyed on the lockfile; CONTRIBUTING.md and a NOTICE file when third-party code is vendored; a README badge once a GitHub remote exists.

## 2026-09-02 S03 Zod schemas for the API contract
Built: packages/schemas implements docs/api.md with zod 4.5.4. Modules: taxonomy.ts (SENSITIVE_TAXONOMY, CATEGORIES_TAXONOMY, SensitiveCategory, ContentCategory), common.ts (AppId, AuditId, CreativeId, Sha256Hash, IsoTimestamp, MessageRole, Message, User, SurfaceType, Surface, ErrorResponse, HealthResponse), classification.ts (ClassificationMethod, Classification), creative.ts (DemandSource, Creative), suppress-reason.ts (FIXED_SUPPRESS_REASONS, SENSITIVE_CATEGORY_REASONS, SUPPRESS_REASONS, sensitiveCategoryReason(), SuppressReason), evaluate.ts (Decision, EvaluateRequest, EvaluateResponse), events.ts (AttestRequest, EventType, EventRequest), verify.ts (VerifyCheckName, VerifyCheck, VerifyResponse), json-schema.ts (CONTRACT_SCHEMAS, toJsonSchemaObject, renderJsonSchema(s), toJsonSchema(outDir?) which writes packages/schemas/json/<Name>.schema.json), gen-json.ts (the gen:json script). 28 JSON Schema files committed. Every TS type is z.infer; index.ts re-exports all modules. 68 tests: all seven docs/api.md bodies verbatim (api-examples.test.ts), taxonomy equals the fixture, SuppressReason accepts the 7 fixed reasons plus sensitive_category:<taxonomy name> and rejects sensitive_category:unknown, EvaluateRequest rejects both messages and context_summary absent, serve/suppress consistency, JSON files byte-identical to a fresh render (verified to fail on a stale file and to be idempotent).
Decisions: zod v4 native z.toJSONSchema (target draft-2020-12, io 'input', unrepresentable 'throw'); no zod-to-json-schema. io 'input' because zod strips unknown keys instead of rejecting, so files carry no additionalProperties:false and stay forward compatible for the Python SDK. JSON files: keys sorted at every level except each `properties` map, which keeps declaration (contract) order; two-space indent; trailing newline; title equals the export name; everything inlined (no $defs) so each file is self-contained. Refinements zod cannot express are attached with .meta() and merged verbatim: EvaluateRequest anyOf required messages|context_summary, EvaluateResponse allOf if/then (serve needs creative object and null reason, suppress needs null creative and string reason). SuppressReason is one flat z.enum (7 fixed + 9 sensitive_category:<name> built from SENSITIVE_TAXONOMY) so the JSON Schema is an explicit enum; core should build reasons with sensitiveCategoryReason(name) for exact typing. Classification.categories is z.array(ContentCategory) and sensitive is z.array(SensitiveCategory), both taxonomy-enforced: the classifier story must normalise LLM output to the taxonomy (unknown category -> general, unknown sensitive flag -> treat as failure/suppress). ClassificationMethod is z.enum(['llm','rules']); prompt_version is any non-empty string; extend the enum only if an error path truly needs it. EvaluateRequest: app_id must start with app_ (prefixed ULID convention; only the prefix is checked because doc examples are elided); user.tier any non-empty string; user.region optional ^[A-Z]{2}$; locale and user_hash optional strings; messages optional but min 1 item when present; context_summary optional min 1; refine requires at least one (issue path ['messages']); surface.placement literal after_answer, max_creatives optional int >= 1 (no default, so input and output types match); policy_overrides is z.record(z.string(), z.unknown()) and S04 should tighten it to a partial PolicyConfig. Unknown keys are stripped, not rejected. EvaluateResponse keeps classification and audit_id required even for reason 'error' (the contract shows them always present): the gateway must synthesise a zeroed classification and still write an audit record on error paths. creative.url is a non-empty string, not z.url(), because the documented example https://<gateway>/c/... is not a parseable URL; Creative.body may be empty, other text fields min 1; DemandSource is direct|affiliate|koah|gravity from docs/policy.md. MessageRole is system|user|assistant|tool. IsoTimestamp = z.iso.datetime() accepts only trailing-Z UTC. Sha256Hash requires the sha256: prefix (AttestRequest.model_output_hash); user_hash stays loose because the doc example is "optional sha256". VerifyCheckName is the eight docs/audit.md checks in order. HealthResponse and ErrorResponse added beyond the story list so every api.md body has a schema. Removed the scaffold packageName export from @adgate/schemas. Dependencies added: zod ^4.5.4 in @adgate/schemas (stack list) and tsx ^4.23.13 at the workspace root so TypeScript dev scripts (gen:json now, seed and migrate scripts later) run without a build step. packages/schemas/json/ is prettier-ignored and listed in package.json files.
Gotchas: Prettier needed a second --write pass on common.ts (member-chain formatting is not idempotent on the first pass); always finish with pnpm format:check. z.toJSONSchema adds maximum 9007199254740991 to .int() and a long regex pattern to z.iso.datetime(); harmless. toJsonSchema() imports node:fs statically from the package entry; if a browser bundle ever imports @adgate/schemas at runtime, move it to a subpath export. `z.string().startsWith()` renders as pattern ^prefix.* in JSON Schema. The agent cwd resets between shell calls; use absolute paths for every file edit.
Ideas: subpath export @adgate/schemas/json-schema to keep node:fs out of the main entry; the gateway /openapi.json (S-gateway) can be assembled from CONTRACT_SCHEMAS via z.toJSONSchema with $defs; PolicyConfig (S04) and AuditRecord (audit story) should be registered in CONTRACT_SCHEMAS so they get JSON files and the staleness test; a coverage threshold for packages/schemas.

## 2026-09-02 S04 PolicyConfig schema, YAML loader, policy hash
Built: packages/schemas: policy-parts.ts (default-free leaf schemas PolicyTier, PolicyRegion, PolicyThreshold, PolicyCap, PolicyRetainDays, AdvertiserDomain, SensitiveDetection, DisclosureLabel/Position/Style, AffiliateNetwork), policy.ts (FrequencyCaps, Disclosure, Privacy, Regions, DemandEntry, PolicyConfig with every docs/policy.md default, PolicyConfigInput), policy-overrides.ts (DemandEntryOverride, PolicyOverrides, OverrideRejection), policy-loader.ts (PolicyValidationError, parsePolicy, parsePolicyYaml). EvaluateRequest.policy_overrides tightened from a loose record to PolicyOverrides. PolicyConfig and PolicyOverrides registered in CONTRACT_SCHEMAS (30 JSON files now). packages/core: canonical/canonicalize.ts, canonical/sha256.ts, policy/hash.ts (policyHash), policy/load.ts (loadPolicyFromYaml), policy/merge.ts (mergeOverrides); scaffold packageName export removed; PolicyValidationError re-exported. fixtures/canonical-json.json added. Tests: schemas 114 (65 new), core 59.
Decisions: Split keeps the dependency direction schemas <- core: schemas owns validation, defaults and YAML parsing (the `yaml` package lives in @adgate/schemas, matching docs/architecture.md "PolicyConfig loader"); core owns canonicalize, policyHash, loadPolicyFromYaml (= parsePolicyYaml + policyHash) and mergeOverrides. Defaults: version is z.literal(1) defaulting to 1; app_id is required and is a free non-empty string (docs use slugs like my-chat-app, not the app_ prefixed AppId); demand defaults to the four documented entries, affiliate entries require network (no default), enabled defaults true per entry; all defaults are factories so every parse returns fresh objects. Validation: z.strictObject at every level; blocked_categories values are SensitiveCategory (the suppress reason sensitive_category:<name> only exists for the sensitive taxonomy); regions.allow matches /^[A-Z]{2}$/ (EU matches, no ISO list check, same rule as User.region); empty serve_to_tiers and regions.allow are accepted (serve nobody = fail closed). JSON Schema: PolicyConfig and PolicyOverrides carry additionalProperties:false (json-schema.test.ts permits it for those two only) plus `contains: {const: self_harm}` and an if/else allOf for the two refinements. Errors: parsePolicy, parsePolicyYaml and loadPolicyFromYaml throw PolicyValidationError with issues [{path, message}] (dotted paths, [n] for indexes, '' for root) and a message "Invalid policy:\n  - path: message"; YAML syntax errors become one root issue prefixed "YAML syntax: ". YAML 1.2 core schema: `yes` stays a string, duplicate keys, multi-document and empty documents are errors. policy_hash = "sha256:" + sha256(canonicalize(policy)) with no re-parse, so only pass fully defaulted PolicyConfig values (parsePolicy, parsePolicyYaml and mergeOverrides outputs). Canonical JSON: hand-assembled string (JSON.stringify per scalar), keys sorted by UTF-16 code unit at every depth (RFC 8785 order; integer-like keys stay lexicographic), undefined properties dropped, any non-JSON value (NaN, Infinity, Date, Map, bigint, function, undefined array element) throws TypeError. Golden: the docs default policy for app_id my-chat-app hashes to sha256:f7443a7f6a44e611bf4deb2d6517341e680f3bae153e7bb3f805ac1d6c0603ae (pinned with its canonical string in packages/core/src/policy/hash.test.ts). mergeOverrides (tighten only, equal values are silent no-ops, inputs never mutated): serve_to_tiers and regions.allow are intersected with the stored list and items the override would add are rejected while its removals still apply; allow_paid_tiers true->false applies and filters serve_to_tiers to free, false->true rejected; blocked_categories and competitor_exclusions are unioned in stored-then-override order, never rejected; sensitive_detection balanced->strict only; min_commercial_intent and min_confidence up only; per_session and per_user_per_day down only; min_turns_between UP only (more turns between ads is stricter, even though docs say "lower caps"); demand entries with enabled:false disable matching stored entries (affiliate without network matches every affiliate entry), enabling a disabled source or naming one the policy lacks is rejected; privacy.store_raw_text true->false only; version, app_id, disclosure.* and privacy.retain_days are per-app settings and any change is rejected. Rejection paths are dotted (frequency_caps.per_session, demand[1]); these become override_rejected notes in the audit story. mergeOverrides takes an already validated PolicyOverrides (EvaluateRequest validates it) and does not re-validate. Shared test fixtures live in non-test modules (packages/schemas/src/policy.fixture.ts, packages/core/src/policy/merge.fixture.ts) because importing a *.test.ts re-registers its tests in the importer. Dependencies added: yaml ^2.9.0 in @adgate/schemas (YAML 1.2 parser, zero deps, actively maintained; needed for parsePolicyYaml); @adgate/schemas workspace:* in @adgate/core (pnpm add writes workspace:^, normalised by hand to workspace:* per the convention).
Gotchas: zod 4 `.default(value)` hands back the same object on every parse, so object and array defaults must be factories. `.partial()` on a schema whose fields carry defaults still fills them in, which is why PolicyOverrides is built from default-free leaf schemas instead of PolicyConfig.partial(). zod 4 runs superRefine only when the shape has no structural issue (missing key, wrong type); value issues (ranges, patterns) co-report with the refinement issues, so tests must not expect the self_harm issue next to a missing key. Function-form defaults on a discriminated-union array need an explicit return type (`(): DemandEntry[] =>`) or TS widens the literals. Object.keys enumerates integer-like keys first, so canonical JSON cannot be JSON.stringify over a re-keyed object. `printf '%s' '<canonical json>' | shasum -a 256` derives golden hashes without a build. Prettier changed line wrapping in eight files after the first write; run it before the gates.
Ideas: a defaultPolicy(app_id) helper; validate regions.allow against the ISO 3166 list and add the EU member-state expansion the policy engine needs; a root `pnpm gen:json` alias; deduplicate list fields inside PolicyConfig; the audit story can reuse sha256Prefixed and canonicalize for record_hash and content_hash.

## 2026-09-02 S05 Policy engine
Built: packages/schemas/src/policy-decision.ts (POLICY_RULES, PolicyRule, PolicyDecisionResult pass|fail|pending, PolicyDecision, CapState; PolicyDecision, PolicyRule and CapState registered in CONTRACT_SCHEMAS, 33 JSON files now). packages/core/src/policy: eu-members.ts (EU_MEMBER_STATES, isEuMemberState), regions.ts (EU_REGION_TOKEN, expandRegions, isRegionAllowed), evaluate.ts (evaluatePolicy, EvaluatePolicyInput, PolicyEvaluation), evaluate.fixture.ts (serveInput, defaultPolicy, SERVE_CLASSIFICATION, FREE_US_USER, FRESH_CAP_STATE, CHAT_SURFACE). Tests: evaluate.test.ts (order, first failure wins, tiers, blocked categories, thresholds, competitor pending, surface), evaluate-caps.test.ts (every cap at its boundary), evaluate-purity.test.ts (import grep), regions.test.ts (EU data, expansion, region rule). Totals: schemas 121 tests (+7), core 110 (+51).
Decisions: Rules run from a Record<PolicyRule, RuleCheck> iterated in POLICY_RULES order; every rule appends exactly one decision and reason is the first fail. Pass entries carry no detail except frequency_caps, which always renders `session=<n>/<cap> day=<n>/<cap> turns_since=<n>` (day=n/a without user_hash, turns_since=none when null) so the docs/audit.md example is reproduced byte for byte; fail entries carry a short detail (tier=paid not in serve_to_tiers, tier=paid requires allow_paid_tiers, region missing, region=BR not in regions.allow, health in sensitive / health in categories, confidence=0.69 min=0.7, commercial_intent=0.59 min=0.6). competitor_exclusions is always { rule, result: 'pending' } with no detail. Details never contain message text. serve_to_tiers re-checks allow_paid_tiers for any non-free tier on top of the schema guarantee (CLAUDE.md non-negotiable). regions: EU expands to the 27 member states (ISO codes, GR not EL); a missing user.region fails closed; a user.region of EU never matches because the token only has meaning inside the policy. blocked_categories iterates policy.blocked_categories in policy order and checks classification.sensitive then classification.categories as plain strings (the content taxonomy has no sensitive names today; the check is defensive as the doc requires both), appending self_harm if a hand-built policy dropped it. Thresholds use `score >= min` and caps use `count < cap` so NaN fails closed: equal to a threshold passes, equal to a cap fails, turns_since_last equal to min_turns_between passes, null passes, per_user_per_day applies only when user.user_hash is a non-empty string. policy.sensitive_detection is not read by the engine: it decides how the classifier merges rule and LLM sensitive signals into classification.sensitive, so the classifier stories own it. surface is accepted and ignored (placement is schema-enforced). evaluatePolicy never throws on typed inputs and does not try/catch malformed ones; the gateway's evaluate wrapper maps any throw to decision suppress reason error. EvaluatePolicyInput and PolicyEvaluation are TS interfaces in core (like MergedPolicy); only the shapes shared with the audit record are Zod. No dependencies added.
Gotchas: exactOptionalPropertyTypes means PolicyDecision.detail must be omitted rather than set to undefined, so decisions are built with a ternary, not a spread. Prettier's wrapping pushed the first evaluate.test.ts past 300 lines; the tests are now split by rule family. A test module that imports a *.test.ts re-registers its tests, so shared inputs live in evaluate.fixture.ts. The purity test walks relative imports from evaluate.ts and asserts the only bare specifier is @adgate/schemas; importing ../canonical (node:crypto) from the engine would fail it on purpose.
Ideas: the gateway should derive turns_since_last as current turn index minus cap_state.last_turn_index (null when no ad yet) and reset day_count on a new UTC day; a renderDecisions helper for the dashboard; validate regions.allow against the ISO 3166 list at parse time; a table-driven fixture file (fixtures/policy-cases.json) so the Python SDK can replay the same cases.
## 2026-09-02 S06 Rules classifier
Built: packages/core/src/classify/rules: normalize.ts (normalizeText: NFKC, lowercase, apostrophes deleted, other punctuation to spaces, whitespace collapsed, whole-token aliases via a Map), match.ts (padded whole-token phrase matching, compiled regex patterns, containsPhrase), score.ts (noisy-OR intent, evidence-count confidence, sensitive confidence), registry.ts (data compiled once, taxonomy order), classify.ts (classifyByRules -> RulesResult), version.ts (computeRulesVersion, RULES_VERSION), types.ts, index.ts. Data: data/sensitive/{health,finance,politics,legal,adult,gambling,weapons,religion,self-harm}.ts, data/commercial/<each of the 18 content categories>.ts, data/aliases.ts, data/intent.ts, data/informational.ts, data/scoring.ts, data/index.ts (SENSITIVE_RULES, COMMERCIAL_RULES, RULES_DATA). Tests: fixtures.test.ts (28 s* cases caught with confidence >= 0.9 and intent <= 0.2, 39 c*/l* cases with no sensitive flag, 7 quickstart devtools cases, aggregate floor), classify.test.ts, normalize.test.ts (case, NFKC, punctuation, contractions, every alias), data.test.ts (hygiene), version.test.ts, purity.test.ts. Core 325 tests (+215). @adgate/core now exports classifyByRules, normalizeText, containsPhrase, RULES_VERSION, computeRulesVersion, RULES_DATA and the rules types.
Decisions: RulesResult is a flat Classification with method 'rules' plus `matches` {sensitive, commercial, intent, informational}; matches hold only dictionary terms and pattern sources, so nothing from the message text leaks, but S08 must still drop `matches` before persisting. QUICKSTART WITHOUT AN LLM KEY: c001 and every software.devtools.* fixture case (c001-c006, c024) reach confidence >= 0.7, commercial_intent inside [intent_min, intent_max] and one of categories_any by rules alone (c001 scores intent 0.992 / confidence 0.95 / categories [database, hosting]); fixtures.test.ts pins this per case, so the default policy (min_confidence 0.7, min_commercial_intent 0.6) serves on rules only. Sensitive is two-tier: any strong phrase or pattern flags the category (confidence 0.9, 0.95 when a second term corroborates); weak phrases flag only when two distinct ones match (confidence 0.75); a single weak hit is reported in matches.sensitive with flagged:false as a hint for S08. Every s* fixture case is a strong match. A sensitive flag caps commercial_intent at 0.2; self_harm forces 0; sensitive categories come out in taxonomy order. Intent = noisy-OR of matched intent phrase weights (strong 0.45, medium 0.3, weak 0.15) plus one category signal (product phrase 0.7, topic phrase 0.2) minus 0.15 per informational phrase (explain, how do i, translate, fix this...), clamped and rounded to 3 decimals. Confidence = 0.3 + 0.15 per distinct matched term (+0.15 when a product matched), max 0.95; no evidence gives 0.3, which fails min_confidence (fail closed). Categories are ordered by score (product 1, topic 0.5), ties in taxonomy order; no hit gives ['general'] (matches the S03 unknown->general rule). Remaining 32 non-devtools serve and low-intent cases: 32 of 32 satisfy their intent bounds and categories by rules alone; the aggregate test prints the number and asserts a floor of 30 so keyword edits do not fail on a single case. No fixture exception was needed: zero sensitive false positives on every c* and l* case. Terms deliberately left out or demoted to weak to protect dev and low-intent text: budget and dollars (finance; they are buying signals), convert (religion; unit conversion), miles (flights), std, stroke and side effects (health; std::vector, CSS stroke, pure functions), bare course, render, modal, cursor, ram, cpu, mouse, max, prime, show, stream, suite, tools, sheets; "insurance" is weak with the specific kinds strong, so pet insurance and travel insurance are products, not finance. RULES_VERSION hashes the canonical JSON of the whole RULES_DATA (phrases, patterns, aliases, intent, informational, scoring), computed once at module init; the test mutates a structuredClone to prove it changes and that key order does not matter. Phrase data must be written in normalized form (data.test.ts) and alias targets are never alias keys (normalize.test.ts). Patterns are regex sources (strings) compiled without flags at init; they count as strong/product hits. packages/core/src/classify/rules/data/ is prettier-ignored because Prettier puts one string per line and would push the lists toward the 300-line limit; purity.test.ts asserts every file under rules/ stays under 300 lines and that the import graph reaches only @adgate/schemas and node:crypto. No dependencies added.
Gotchas: The Bash tool rejects a command whose text contains certain escape sequences ("\r\n" inside a heredoc was refused as hidden control characters); the Write tool is the fallback. A tsx script outside packages/* cannot resolve @adgate/schemas (exports map), so run dev scripts from inside the package. vitest it.each over object arrays supports $id in test names. The fixture path from packages/core/src/classify/rules is ../../../../../fixtures/classify-fixtures.json. Prettier only re-wrapped three files; the data directory is ignored on purpose.
Ideas: light stemming so plurals need not be listed by hand; per-term weights in the data files; a negative-context list to unflag phrases like nerf gun or glue gun; a fixture of joined multi-turn conversations once S08 lands; expose matches in the dashboard as a "why suppressed" view; a script that prints rules-only results for the whole fixture to tune lists.

## 2026-09-02 S07 LLM classifier with timeout and versioned prompt
Built: packages/core/src/classify/llm: types.ts (LlmClassifier, LlmClassifyResult/Success/Failure, LlmClassifyOptions, LlmClassifierConfig, the LlmFetch/LlmFetchInit/LlmFetchResponse slice of fetch, LLM_FAILURE_REASONS, DEFAULT_LLM_TIMEOUT_MS = 400), prompt.ts (CLASSIFIER_PROMPT, computePromptVersion, PROMPT_VERSION), output.ts (parseLlmContent, toLlmClassification, normalizeCategories, normalizeSensitive), openai.ts (OpenAiCompatibleClassifier, CHAT_COMPLETIONS_PATH, buildChatCompletionsUrl, buildChatCompletionsBody), fake.ts (FakeLlmClassifier, fakeLlmSuccess, fakeLlmFailure), fixtures.ts (fakeLlmFromFixtures, fixtureLlmResult, unknownTextLlmResult, FIXTURE_LLM_CONFIDENCE, ClassifyFixtureCase/Expect), index.ts; all re-exported from @adgate/core. packages/schemas/src/classification.ts gains LlmOutput. Tests: prompt.test.ts (sha256 of the exported text, changes on edit, lists every taxonomy value), output.test.ts, openai.test.ts (timeout with fake timers incl. a fetch that ignores abort and a slow body, parse, invalid, http, network, aborted, default globalThis.fetch wiring), openai-request.test.ts (URL, bearer, model, temperature, response_format, messages, happy path with taxonomy normalisation), fake.test.ts (modes, call recording, fixture script over all 67 cases), purity.test.ts. Core 381 tests (+56), schemas 125 (+4).
Decisions: LlmOutput is a Zod schema in @adgate/schemas (not CONTRACT_SCHEMAS) because core imports only @adgate/schemas and node:crypto and has no zod dependency; the llm purity test pins that import graph. Numbers are clamped into [0,1] BEFORE validation (1.02 -> 1, -0.1 -> 0), non-numbers, missing keys and non-string list items are reason 'invalid'; list values are trimmed, lowercased, spaces and hyphens become '_' ("Self-Harm" -> self_harm), unknown values are dropped, duplicates removed, empty categories -> ['general']. Reasons: response body not JSON or no choices[0].message.content string -> parse; content not JSON -> parse; content fails LlmOutput -> invalid; non-2xx -> http (detail "HTTP <status>"); fetch rejection, synchronous throw or body read failure -> network (detail "<name>: <message>" cut at 200 chars); timer -> timeout; caller signal -> aborted (before the call: latency_ms 0, fetch not called). detail never carries message or model text: JSON.parse messages quote the input in Node 20 so a fixed string is used, and schema issues are reported as "path: code". Timeout is Promise.race between the request and a timer that also aborts an internal AbortController, so classify settles at timeoutMs even if the transport ignores the signal; the caller's signal is linked to the same controller and unlinked in finally so no timers or listeners leak. latency_ms is Date.now() based (integer, fake-timer friendly). Request: POST <baseUrl with trailing slashes trimmed>/chat/completions, Content-Type application/json, Authorization: Bearer <apiKey> omitted when apiKey is '' (local endpoints), body { model, temperature: 0, response_format: { type: 'json_object' }, messages: [system CLASSIFIER_PROMPT, user text] }, no max_tokens (some compatible endpoints reject it; the timeout bounds latency). Content wrapped in a markdown fence is unwrapped before parsing. The default fetch is a wrapper around globalThis.fetch (not a captured reference) so vi.stubGlobal works after construction; every llm test stubs it to reject. FakeLlmClassifier: 'timeout' returns reason timeout with latency 400, 'parse' returns reason parse, 'throw' rejects with "FakeLlmClassifier: throw mode" so S08 can prove a throw becomes rules-only; calls[] holds every text, plus callCount and reset(). fakeLlmFromFixtures: intent = midpoint of [intent_min, intent_max] when both exist, else 0.9 for a sensitive-less case, 0.1 for a sensitive case, 0 for self_harm; categories = normalized categories_any or ['general']; sensitive = expect.sensitive normalized; confidence 0.9; unknown text -> intent 0.1, ['general'], confidence 0.5. Matching is normalizeText exact first, then the longest fixture text contained in the input, so a joined multi-turn conversation still hits its fixture. ClassifyFixtureCase is a TS interface (test data, not a contract). The prompt is paragraphs joined with blank lines, built from CATEGORIES_TAXONOMY and SENSITIVE_TAXONOMY, so a taxonomy edit changes PROMPT_VERSION too; no test pins the current value (sha256:6a94e128...) on purpose. Gateway env wiring belongs to the gateway story (variables already exist in .env.example). No dependencies added.
Gotchas: purity.test.ts greps implementation sources for the literal process.env, so even a doc comment mentioning it fails. vi.fn(async () => ...) infers zero parameters and mock.calls[0][1] is then a tsc error; type the fake as vi.fn<LlmFetch>(impl). Joining fixed-width prompt lines with '\n' split "never answer the user" across lines and failed the wording test; hence the paragraph() helper. Vitest fake timers also mock Date, so latency_ms in timeout tests equals timeoutMs exactly. Cross-package: after adding LlmOutput to schemas, run `pnpm --filter @adgate/schemas build` before running core's vitest directly. The Write tool emitted a NUL byte in place of a single-space string literal (' ') in output.test.ts; tests still passed but git showed the file as `Bin` in `git show --stat`. Check the commit stat for Bin entries on .ts files and grep for \x00 before committing.
Ideas: an Anthropic Messages API LlmClassifier as a second implementation; optional max_tokens in the config; return the model's usage block for cost dashboards; one retry on 429 within the remaining deadline; pre-serialize the system message; a NEEDS-LLM-KEY smoke script that hits a real endpoint with the fixture set and prints precision, run manually only.

## 2026-09-02 S08 classify() orchestrator with cache and merge
Built: packages/core/src/classify: types.ts (ClassifyInput, ClassifyPolicy, ClassifyCache, ClassifyDeps, ClassifySource, ClassifyLlmFailure, ClassifyOutcome), prepare.ts (prepareText, DEFAULT_PREPARE_OPTIONS), cache.ts (classifyCacheKey, createLruCache, LruCache, LruCacheOptions, DEFAULT_CACHE_MAX_ENTRIES, DEFAULT_CACHE_TTL_MS), merge.ts (mergeClassifications, strongSensitiveFlags, rulesFired), classify.ts (classify, failClosedClassification), index.ts; all re-exported from @adgate/core. @adgate/schemas ClassificationMethod gains 'cached' (Classification, ClassificationMethod and EvaluateResponse JSON files regenerated). Tests: classify-fixtures.test.ts (full golden run under strict and balanced with FakeLlmClassifier(fakeLlmFromFixtures): 28/28 sensitive recall, 0 false positives on 39 c*/l* cases, every c* inside intent bounds with a listed category and confidence >= 0.7, every l* under intent_max, second pass 67/67 from cache with zero LLM calls, plus a rules-only run with llm null), classify.test.ts, classify-fallback.test.ts, classify.fixture.ts, prepare.test.ts, cache.test.ts, merge.test.ts, purity.test.ts. Core 461 tests (+80), schemas 125.
Decisions: Flow is prepare -> cache lookup (hit returns a copy with method 'cached', skipping rules and LLM) -> classifyByRules -> short circuit when any flagged sensitive match has strength 'strong' (rules confidence >= 0.9; self_harm intent 0, others capped at 0.2 by the rules stage) -> blank text skips the LLM (llm_failure 'empty_input') -> llm null skips it ('unavailable') -> LLM -> merge -> cache. Merge: LLM wins on categories and commercial_intent; confidence is the LLM's unless rules fired (any flagged sensitive category or any commercial category match, product or topic), then min(rules, llm); method 'llm' and prompt_version PROMPT_VERSION. BALANCED MODE DEFINITION (docs/policy.md leaves it open): strict = union of rules and LLM sensitive flags; balanced = strong rule flags (a strong phrase or pattern hit) plus the LLM's flags only when the LLM confidence >= policy.min_confidence, so weak-pair rule hits (confidence 0.75) and unsure model flags do not suppress on their own. Merged sensitive lists are in taxonomy order; a non-empty list caps commercial_intent at RULES_DATA.scoring.sensitive.max_intent (0.2) and self_harm forces 0, mirroring the rules stage. The cache key includes sensitive_detection and min_confidence on top of text + PROMPT_VERSION + RULES_VERSION because the merged result depends on them and the gateway will share one process-wide cache across apps with different policies. Rules-only fallbacks (any LLM failure, llm null, empty input, or an internal throw) are never cached; short-circuit and merged results are, as copies, and hits are returned as copies so callers cannot poison the cache. classify never throws: an inner catch turns any throw (a cache get or set that throws, an LLM that throws synchronously or rejects) into the rules-only classification with llm_failure 'thrown' (rules are re-run if they had not completed); if the input cannot even be prepared the outcome is failClosedClassification() with cache_key ''; an outer catch covers a throwing clock. A broken cache is deliberately not isolated (simplest code, consistent with the story), so a Postgres ClassifyCache must swallow its own errors and return undefined. ClassifyOutcome adds latency_ms (from deps.now, default Date.now) beyond the story's fields because the gateway needs it for the response latency budget; deps.signal is forwarded to the LLM stage for the same reason. prepareText keeps 'role: content' lines so the LLM sees who spoke; truncation counts code points so surrogate pairs are never split; context_summary gets the same last-4,000 rule. On the fixture the min(rules, llm) confidence rule leaves every c* case at 0.9 (rules 0.95); l007 ('translate good morning to Japanese', a topic-only education.language hit with rules confidence 0.6) drops to 0.6, which still suppresses. No dependencies added.
Gotchas: Property access on a primitive does not throw, so classify(42) or classify('nope') just classifies the empty string; the never-throws test needs null or a non-array messages value to make prepareText throw. classify.test.ts crossed 300 lines after Prettier and was split by responsibility, with shared inputs in classify.fixture.ts (purity tests treat *.fixture.ts as non-implementation). Turbo caches builds, so after editing schemas run pnpm build (or pnpm --filter @adgate/schemas build) before running core's vitest directly. The Bash tool's cwd persisted inside the classify directory across one call, so relative prettier globs matched nothing; use absolute paths.
Ideas: isolate cache get/set failures as misses once a Postgres cache exists, if a flaky DB ever degrades too many turns to rules-only; exclude topic-only rule matches from 'rules fired' if real traffic shows serve turns failing min_confidence after the merge; hit/miss counters on the LRU for the Prometheus story; a Postgres-backed ClassifyCache keyed by cache_key with the same TTL; a multi-turn fixture (assistant answers included) to tune the role-prefixed text; expose ClassifyOutcome.source in the audit record's classification detail for the dashboard.
## 2026-09-02 S09 DemandAdapter interface and DirectAdapter
Built: packages/schemas/src/demand.ts (TARGET_CATEGORY_PATTERN, TargetCategory, CatalogCreative, SeedCreative, DemandUser, DemandRequest, Candidate, DemandResponse; CatalogCreative, SeedCreative, TargetCategory, DemandRequest, DemandResponse and Candidate registered in CONTRACT_SCHEMAS, 39 JSON files now). packages/core/src/ids/ulid.ts (ULID_PATTERN, ULID_LENGTH, ULID_TIME_MAX, encodeUlidTime, encodeUlidRandom, ulidTime, isUlid, ulid, prefixedUlid, UlidOptions). packages/core/src/demand: types.ts (DemandAdapter, DemandFetchOptions, DirectAdapterOptions, DEFAULT_DEMAND_TIMEOUT_MS = 250), regions.ts (creativeServesRegion), keywords.ts (normalizeKeywords, keywordMatches, matchedKeywords, keywordOverlap, isPatternSource, keywordsFromRulesMatches), match.ts (isWildcardTarget, matchesTargetCategory, categoryScore, targetingScore, weight constants), catalog.ts (CREATIVE_ID_PREFIX, creativeId, assignCreativeIds), direct.ts (DirectAdapter, createDirectAdapter, selectDirectCandidates, compareCandidates, DIRECT_MAX_CANDIDATES = 3), index.ts, direct.fixture.ts; everything re-exported from @adgate/core. Tests: schemas 140 (+15: demand.test.ts incl. the seed file), core 512 (+51: ulid, regions, keywords, match, catalog, direct, purity, index).
Decisions: CatalogCreative = { id cr_, advertiser, advertiser_domain, headline, body (may be empty), cta, url_template, target_categories: TargetCategory[], target_regions: PolicyRegion[] (^[A-Z]{2}$, EU allowed), keywords: string[] (each min 1), ecpm >= 0, source: DemandSource, active, network?: AffiliateNetwork, program_id? }; no defaults so input and output types match (hand-built catalogs spell out every list). SeedCreative = CatalogCreative.omit(id); examples/creatives.seed.json parses as is (its affiliate entry has no network/program_id; S10 decides how those are supplied). TargetCategory is regex-only (/^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)*(\.\*)?$/), not checked against the taxonomy, so wildcard prefixes need not be taxonomy nodes. DemandRequest.user is User.pick(region, locale): tier and user_hash never reach adapters. DemandRequest.keywords are dictionary terms only: keywordsFromRulesMatches(rulesResult.matches) keeps the commercial phrase terms (regex pattern sources dropped), normalized and de-duplicated; the gateway must never pass message text (privacy). DemandAdapter.fetch never rejects: an already aborted signal returns { candidates: [], error: 'aborted' }; any throw (malformed input) returns error '<Name>: <message>' cut at 200 chars. DirectAdapter copies the catalog at construction, serves only source === 'direct' (each adapter owns its source, so the gateway may hand every adapter the whole creatives table) and active creatives; region: empty target_regions = everywhere (also serves a request without a region), otherwise isRegionAllowed(region, target_regions) from policy/regions (EU expands to the 27 member states, a user region of EU never matches, a missing region fails closed for targeted creatives). A category match is REQUIRED; keyword-only hits are never candidates. Exact category hit scores 1, trailing-wildcard hit 0.8 (software.devtools.* matches software.devtools.database and deeper, never software.devtools itself or software.security); best across targets x categories, no position weighting. keyword overlap = fraction of the creative's distinct normalized keywords that some request keyword equals or contains as whole tokens (either direction, via normalizeText + containsPhrase, so aliases apply). targeting_match = round3(0.75 * category + 0.25 * keyword), so direct candidates lie in [0.6, 1]; ecpm_estimate = ecpm; resolved_url = url_template (direct URLs have no placeholders; the click redirect should prefer resolved_url when present). Order: targeting_match desc, ecpm_estimate desc, id asc (code-unit compare); top 3. competitor_exclusions are NOT applied by adapters: S11 mediation drops them so the audit trace can list `excluded`. timeoutMs is accepted and unused (synchronous, no I/O). latency_ms comes from an injected `now` (default Date.now like classify; Vitest fake timers mock Date but not performance, so performance.now was rejected), rounded, clamped at 0, 0 when the clock throws. Golden numbers on the seed: c001 with categories ['software.devtools.database'] and rules keywords ['postgres hosting', 'postgres', 'hosting'] -> Example DB Cloud 0.85 (exact, 2/5 keywords) then Example Deploy 0.65 (wildcard, 1/5); with the full rules classification [database, hosting] Deploy scores 0.8 and DB Cloud still wins; without keywords DB Cloud 0.75. ULIDs are a hand-written Crockford base32 encoder (no `ulid` dependency): 10 time chars (48-bit ms) + 16 random chars (80 bits), injectable now/random (defaults Date.now and node:crypto randomBytes), no monotonic guarantee within one millisecond; prefixedUlid('cr_') / creativeId(); assignCreativeIds(seeds, ulidOptions?) maps seeds to CatalogCreative in order without mutating them. S17's seed script should use SeedCreative.array().parse + assignCreativeIds and prefixedUlid for app_/key_/aud_/ev_/adv_ ids. demand/purity.test.ts pins the import graph of demand/index.ts to @adgate/schemas + node:crypto (direct.ts alone reaches only @adgate/schemas). No dependencies added.
Gotchas: normalizeText expands "don't" to "do not" through the alias table, so keyword tests must expect alias targets. A test-local `{ categories: [...] }` literal widens to string[] and fails Pick<Classification, 'categories'>; annotate it. ESLint flags a `_matches` rest-destructuring discard as unused; use the value. After editing schemas run pnpm --filter @adgate/schemas build before running core vitest directly. Prettier accepted every new file on the first pass.
Ideas: position-weight classification categories (most relevant first) in categoryScore; treat empty target_categories as a low-score run-of-network match; a DemandTrace schema in @adgate/schemas for the audit `demand` block (S11); per-app catalogs keyed by app_id inside DirectAdapter; a Postgres-backed catalog loader with an in-memory refresh interval in the gateway; monotonic ULIDs if a seed script ever needs strict order inside one millisecond; a domain regex plus lowercasing on advertiser_domain so competitor_exclusions compare case-insensitively.

## 2026-09-02 REVIEW-1 Fixes from the S03-S08 code review
Built: Applied findings 1-6, 8-14, 16 and 17 of the review of 1ae63cb..262280b, each with a failing test first. (1) classify/merge.ts: balanced mode keeps every flagged rule category (weak pairs included) and adds LLM flags only at confidence >= min_confidence; self_harm is never dropped by either mode (merge.test.ts covers weak pair kept, LLM-only self_harm at 0.65 kept, LLM-only health at 0.65 dropped in balanced and kept in strict). (2) policy/merge.ts: regions.allow overrides compared on expanded countries (DE kept on a stored EU, ['EU','DE'] kept on a stored ['EU'], ['BR'] on the default rejected with the stored list kept). (3) classify.ts: deps.timeoutMs (default DEFAULT_LLM_TIMEOUT_MS) races the LLM call; expiry is rules_fallback + llm_failure 'timeout'; classify-deadline.test.ts proves it with fake timers and a FakeLlmClassifier 'hang' script, and that no timer survives any outcome. (4) EvaluateRequest: messages lost .min(1); the refinement is non-empty messages OR non-empty context_summary; { messages: [], context_summary: 'x' } parses, { messages: [] } alone fails; JSON files regenerated. (5, 14) prepareText -> { text, rulesText }: only user and assistant messages are classified, the rules stage gets bare content (a tool message can no longer fire the 'tool' intent phrase). (6) The empty-input guard tests rulesText, so '   ' or a system-only request never calls the LLM. (8) LlmClassifierConfig.fetch is required; openai.ts has no globalThis.fetch. (9) apr, dental and naked demoted to weak; weapons gained an exclusions list (glue/nail/heat/staple/spray/water/paint gun and friends) masked out before matching, with 'gun' still strong. (10) examples/policy.example.yaml lists koah and gravity disabled; both loader tests compare against PolicyConfig.parse({ app_id: 'example-chat' }). (11) ClassifyFixture Zod schema parses fixtures/classify-fixtures.json in every fixture test. (12) lastChars slices to 2 * maxChars UTF-16 units before Array.from; a 1 MB message still yields the last 4,000 code points. (13) ClassifyOutcome.error_name carries the thrown Error's name only. (16) toJsonSchema and JSON_SCHEMA_DIR moved to packages/schemas/src/json-schema-files.ts; index-purity.test.ts pins the index import graph to zod + yaml. (17) openai.test.ts asserts vi.getTimerCount() === 0 after a success and after a failure. Tests: schemas 147 (+7), core 553 (+41); the fixture contract is unchanged (28/28 sensitive recall, 0 false positives on c*/l*, every devtools case >= 0.7 by rules alone, rules-only 32/32).
Decisions: BALANCED MODE DEFINITION (supersedes S08): strict = union of all rules-flagged categories and all LLM sensitive categories; balanced = union of all rules-flagged categories (strong and weak-pair, anything the rules stage reports as flagged) and the LLM's sensitive categories whose LLM confidence >= policy.min_confidence; in both modes self_harm from any source at any confidence is never dropped. Regions: an override entry is kept when its expansion is a subset of the stored expansion and rejected only when it adds a country; entries are de-duplicated and kept in override order; an override rejected in full leaves the stored list untouched (an override whose every entry adds a country is a client mistake, not a request to serve nowhere; an explicit [] still applies), while serve_to_tiers keeps the S04 plain intersection (see Ideas). ClassifyFixture lives in @adgate/schemas rather than under packages/core/src/classify as the finding suggested: core has no zod dependency and its three purity tests ban zod, and schemas already owns validation; its expect.sensitive and categories_any are taxonomy-typed so a fixture typo fails at parse time (fake.test.ts lost its 'nonsense' flag case; unknown-value dropping is covered by output.test.ts). The node:fs writer moved to a module (json-schema-files.ts) that gen-json.ts imports, not into gen-json.ts itself, so json-schema.test.ts can import the writer without running the script (no main-module guard). The pre-slice factor is 2, not the suggested 4: a code point is at most two UTF-16 units, and when the slice starts on the low half of a pair that half is one extra point at the front which the final slice drops (tested). prepareText falls back to context_summary when every message has a dropped role, and dropped roles do not consume one of the four slots. error_name is 'NonError' for a thrown non-Error. The orchestrator deadline only races; it does not abort the transport (the OpenAI client's own timeout, default 400 ms too, does that). FakeLlmClassifier gained 'hang'. The types ClassifyFixtureCase/ClassifyFixtureExpect are no longer exported from @adgate/core (import them from @adgate/schemas). user.region only gained a .describe() saying absence fails closed as region_blocked (finding 15 skipped, see Ideas).
Gotchas: A zsh glob in a grep --include pattern made the first usages search return nothing; quote the pattern. Prettier re-wrapped six test files after the first write; format:check must run last. The turbo build cache makes `pnpm build` a no-op when nothing changed, so after editing schemas run `pnpm --filter @adgate/schemas build` before running core's vitest directly. Vitest fake timers advance Date.now with the timers, which is why the deadline test can assert latency_ms 100 exactly. vi.getTimerCount() after an awaited classify that resolved through microtasks only is the cheapest leak check for Promise.race timers.
Ideas: (7) SKIPPED: json_schema strict response mode for the OpenAI client, because most OpenAI-compatible endpoints (Ollama, vLLM, Groq) reject it and it cannot be verified offline; revisit with a NEEDS-LLM-KEY smoke script. (15) SKIPPED: making user.region required; it stays optional because absence already fails closed as region_blocked, which is the documented behaviour, so only the field description now says so. Align serve_to_tiers with the regions rule (an all-rejected override keeps the stored list) if the asymmetry bites. Abort the LLM transport from the orchestrator deadline through a linked AbortController if leaked in-flight requests show up under load. A per-category exclusions list for health and finance (e.g. 'stroke width', 'credit roll') if real traffic surfaces more compounds. Export a `region_blocked` note in the audit record when user.region was absent so app developers see why.

## 2026-09-02 S10 AffiliateAdapter link builders
Built: packages/schemas/src/affiliate.ts (PartnerStackConfig, ImpactConfig, AMAZON_MARKETPLACES, AmazonMarketplace, AmazonConfig, AffiliateConfig; AffiliateConfig and AffiliateNetwork registered in CONTRACT_SCHEMAS, 41 JSON files now; CatalogCreative.network and program_id descriptions now state the missing-network and override semantics, four demand JSON files regenerated). packages/core/src/demand/affiliate: types.ts, template.ts (TEMPLATE_PLACEHOLDERS, fillTemplate, isHttpUrl), partnerstack.ts, impact.ts, amazon.ts (buildAmazonUrl, isAmazonHost, setTagParam), registry.ts (AFFILIATE_BUILDERS, AFFILIATE_NOT_CONFIGURED, isAffiliateConfigured, buildAffiliateUrl), adapter.ts (AffiliateAdapter, createAffiliateAdapter, selectAffiliateCandidates, defaultDestination, AFFILIATE_MAX_CANDIDATES), index.ts; select.ts and response.ts factored out of direct.ts and shared by both adapters; everything re-exported from @adgate/core. Every adapter and builder file opens with a comment that the app owner brings their own affiliate accounts and adgate holds no credentials or payouts. Tests: schemas 152 (+5), core 603 (+50: template, partnerstack, impact, amazon, registry, adapter, adapter-selection incl. DirectAdapter parity, select; purity now walks affiliate/).
Decisions: Placeholder values are encodeURIComponent'd wherever they sit (query, path or host): ids are identity-encoded and a destination is safe inside a query parameter; a template that is only {{u}} is unsupported on purpose. Unknown placeholders fail (unknown_placeholder:<name>) instead of passing through, so a typo can never ship an untracked link; the result must parse as an absolute http(s) URL (invalid_url) because the click redirect will 302 to it. Amazon: the owner's tag always replaces any tag in the template (a catalog entry can never credit another account), amzn.to short links and non-www subdomains are rejected, and config.marketplace when set must match the host because Associates tags are storefront-specific; AMAZON_MARKETPLACES lists the 21 storefront suffixes. The adapter fills {{u}} with https://<advertiser_domain>/ because CatalogCreative has no destination field (adding one is out of scope; the seed keeps its destination literal). A creative-level program_id overrides the config id (or the amazon tag) but never substitutes for a missing network entry, so affiliate_not_configured depends on AffiliateConfig alone. Build failures are per creative and travel alongside the surviving candidates in DemandResponse.error (build_failed:<id>:<reason>, comma-joined, cut at 200 chars): S11 must not read a set error as "no candidates". AffiliateConfig is z.strictObject like PolicyConfig (config-time input, not wire input) and json-schema.test.ts now allows additionalProperties:false for it. The adapter structuredClones its config and copies its catalog at construction; fetch never rejects (aborted -> 'aborted', unconfigured -> 'affiliate_not_configured', any throw -> '<Name>: <message>'). No dependencies added.
Gotchas: A `let failure: string | undefined` assigned inside a String.replace callback is narrowed to undefined by TS after the call; collect failures in an array. The persisted shell cwd struck again: after a `cd packages/core/src/demand` in one call, the next call's `pnpm build` and `pnpm test` ran inside that directory (silently core-only) and `pnpm format:check` was "not found"; cd to the absolute repo root before every gate. Prettier pushed the adapter test past 300 lines (334); it is split into adapter.test.ts, adapter-selection.test.ts and adapter.fixture.ts. A recursive directory walk in purity.test.ts must relativize once at the top level, not inside the recursion (nested paths came back as ../../affiliate/...). isAmazonHost takes marketplace as a plain string so tests can probe non-enum values; the config type still narrows it to AmazonMarketplace.
Ideas: a destination_url field on CatalogCreative so {{u}} templates can deep-link per creative; SubID passthrough ({{subid}} = audit id) so owners can reconcile conversions per impression once the click redirect exists; validate the Associates tag format per storefront (e.g. the -20 suffix on .com); a dashboard warning when an affiliate creative names a placeholder the app's config cannot fill; catalog-time validation that rejects amzn.to templates before they reach the adapter.

## 2026-09-02 S11 mediate() across adapters
Built: packages/schemas/src/demand-trace.ts (DemandResponseSummary = DemandResponse.extend({ candidates: int >= 0 }), ExcludedCandidate { source, creative_id, advertiser_domain, reason: 'competitor_exclusion' }, DemandTrace { requested, responses, excluded, selected: DemandSource | null }; all three registered in CONTRACT_SCHEMAS, 44 JSON files now; demand-trace.test.ts parses the docs/audit.md `demand` block verbatim). packages/core/src/demand/exclusions.ts (normalizeDomain, domainMatches, isExcludedDomain) and mediate.ts (mediate, mediationScore, compareByRevenue, MEDIATION_TIMEOUT_ERROR, MEDIATION_ABORTED_ERROR, ADAPTER_ERROR_PREFIX, MediateOptions, MediationResult); select.ts exports compareIds; demand/index.ts and @adgate/core re-export everything. Tests: schemas 160 (+8), core 632 (+29: exclusions, mediate (trace shape incl. the audit example byte for byte, exclusions, ranking), mediate-timing (a hanging adapter never delays a fast one past timeoutMs, parallel settle at the slowest not the sum, default and bad timeoutMs, late answers ignored, all adapters failing, error names only, cancellation, garbage input), index; purity file list updated, mediate.fixture.ts FakeAdapter).
Decisions: mediate(adapters, req, policy, { timeoutMs = 250, now?, signal? }) -> { selected, selected_source, trace }; policy is Pick<PolicyConfig, 'competitor_exclusions'> so a full PolicyConfig or a bare object both work, and mediate never reads policy.demand: the gateway chooses and orders the enabled adapters (trace.requested = their sources, repeats allowed). Each adapter gets its own setTimeout and AbortController; the timer resolves the per-adapter race with { candidates: 0, latency_ms: timeoutMs, error: 'timeout' } and aborts the adapter's signal, so a slow source costs at most timeoutMs and its late answer is dropped; Promise.allSettled joins the races (a rejected outcome, which the wrapper makes near impossible, is still mapped to adapter_error). Rejections, synchronous throws and non-DemandResponse answers become 'adapter_error:<Error.name>' (NonError for non-Errors or an empty name, InvalidResponse for a missing candidates array): error names only, never messages, because the trace is signed into the audit record. An adapter's own error string is copied (cut at 200 chars, dropped when empty) and its candidates still compete (S10 build_failed semantics). latency_ms is measured by the mediator's clock from dispatch to settle, NOT copied from DemandResponse.latency_ms: the audit should carry what the gateway observed, a third-party adapter must not self-report, and latencySince guarantees the integer >= 0 invariant of the schema. Exclusions are the union of policy.competitor_exclusions and req.exclusions, matched on normalizeDomain (trim, lowercase, trailing dot stripped) as equal or subdomain; excluded entries are appended in requested-then-candidate order; responses[].candidates keeps the adapter's count so the trace shows both what was offered and what was dropped. Ranking is compareByRevenue (score desc, ecpm desc, id asc) over the eligible candidates of every source; ties across sources therefore fall to the id, not to demand order. ExcludedCandidate.reason is a z.literal (widen to an enum when a second reason exists). Caller signal already aborted: no adapter is called and every entry is { candidates: 0, latency_ms: 0, error: 'aborted' }; aborted mid-flight: pending adapters settle at once with 'aborted' and their latency so far, finished ones keep their answers. timeoutMs that is NaN, negative or non-finite falls back to the default, a fractional one is rounded, a throwing clock yields latency 0, and adapters/policy that are not what the types promise fall to a no-fill result (requested = the sources when readable, else []). No dependencies added.
Gotchas: ESLint prefer-const rejects `let timer` assigned once after a hoisted `function finish` reads it; resolving the already-aborted case before the timer exists lets the timer be a const. vi.getTimerCount() throws when timers are not faked, so both mediate test files install fake timers in beforeEach and assert 0 timers in afterEach; a fake adapter whose delay is longer than the timeout leaves ITS OWN timer pending after mediate settles, so that test drains with vi.runAllTimersAsync(). vi.advanceTimersByTimeAsync(0) right after controller.abort() is enough for the abort path to settle. Prettier only re-wrapped, nothing crossed 300 lines (mediate.ts 235, mediate.test.ts 267).
Ideas: validate third-party adapter answers with DemandResponse.safeParse at the gateway boundary (core has no zod); return the full ranked list for surface.max_creatives > 1; count 'timeout' vs 'aborted' vs 'adapter_error' per source as gateway metrics; let mediate accept policy.demand plus an adapter registry and do the enabled/ordering itself once S12 fixes the koah/gravity constructor shapes; a per-source ecpm floor or a policy-level minimum score before a creative may win.

## 2026-09-02 S12 Koah and Gravity adapter stubs
Built: packages/schemas/src/network-adapters.ts (KoahConfig, GravityConfig from one strict-object factory, NetworkAdapterConfig union type; registered in CONTRACT_SCHEMAS and the package index, 46 JSON files now, json-schema.test.ts strict list extended). packages/core/src/demand/network-stub.ts (NETWORK_NOT_CONFIGURED, NETWORK_NOT_IMPLEMENTED, NetworkAdapterOptions, isNetworkConfigured, NetworkStubAdapter<Source>), koah.ts (KoahAdapter, createKoahAdapter) and gravity.ts (GravityAdapter, createGravityAdapter), each opening with a header that names docs/decisions.md item 10 and the story-allowed TODO block (request: app_id, classification categories, region, locale, surface, max_candidates, exclusions; response: creative fields plus ecpm_estimate and a targeting_match basis; budget via opts.timeoutMs/opts.signal; `Partner docs: TBD`); all re-exported from demand/index.ts and @adgate/core. Tests: schemas 171 (+11: network-adapters.test.ts), core 657 (+25: network-stub.test.ts covering both stubs via describe.each, disabled / enabled-without-credentials -> not_configured, enabled-with-credentials -> not_implemented with a stubbed global fetch asserted uncalled, injected clock, aborted signal, garbage options never reject, no credential on the instance, header checks read from disk by the test, and mediate() with disabled and with enabled stubs next to a DirectAdapter (direct still wins; stubs alone are no fill); purity file list plus a no-fetch(/no-globalThis assertion for koah.ts and gravity.ts; index).
Decisions: One shared NetworkStubAdapter instead of two copies so the sources cannot drift; KoahAdapter/GravityAdapter are thin subclasses typed NetworkStubAdapter<'koah'> / <'gravity'> so `source` stays a literal. Config is a plain object, never env (core stays pure; BUILD_GUIDE's "read config from env" belongs to the gateway wiring story). Not configured = enabled !== true OR api_key/base_url missing or blank; the schema already rejects empty strings, the adapter is defensive because the gateway may build the object by hand. Enabled with both credentials answers 'not_implemented' instead of pretending: no request shape is guessed and no network is touched (docs/decisions.md item 10). The adapter stores only the configured boolean, so a serialized adapter or a describeError message can never carry a credential. The aborted check precedes the configured check, like DirectAdapter and AffiliateAdapter. Error strings are bare codes ('not_configured', 'not_implemented') because the trace already names the source. api_key and base_url are z.string().min(1) rather than a URL check: the endpoint shape is unknown until partner docs exist. No dependencies added. No env vars added to .env.example because nothing reads them yet; the wiring story adds KOAH_*/GRAVITY_* (see Ideas).
Gotchas: A perl -0pi program written inside shell double quotes swallowed `${name}` in a test template literal (the shell expanded it to an empty string); write perl programs in single quotes. Prettier wants a generic class's type parameter list broken across lines before `implements` when the header exceeds the width. Zod 4's toJSONSchema with io: 'input' renders `.default(false)` as an optional property carrying "default": false, so KoahConfig.schema.json needed no special casing. The docs/decisions.md item 10 header check must read the file with node:fs from the test only; the adapters' purity test would fail otherwise.
Ideas: gateway wiring: KOAH_ENABLED/KOAH_API_KEY/KOAH_BASE_URL and GRAVITY_* parsed with KoahConfig/GravityConfig at boot, stubs handed to mediate() only when the policy demand entry is enabled, and a per-source `configured` flag in metrics so an operator can see a disabled stub; when partner access arrives, keep the transport injected like the LLM classifier (never a platform fetch in core), validate answers with DemandResponse.safeParse at the gateway boundary and record one fixture per partner so unit tests stay offline; a policy-level warning when a demand entry is enabled but its config is not.

## 2026-09-02 S13 Hashing and Ed25519 signing utilities
Built: packages/schemas/src/keys.ts (KEY_ID_PATTERN + KeyId, ED25519_SIGNATURE_PATTERN + Ed25519Signature, PUBLIC_KEY_PEM_PATTERN + PublicKeyPem, PublicKeysJson = z.record(KeyId, PublicKeyPem); all four registered in CONTRACT_SCHEMAS, 50 JSON files now; json-schema.test.ts gained a `records` list because a z.record renders additionalProperties as its value schema). packages/core/src/audit: crypto.ts (sha256Hex/sha256Prefixed over string | Uint8Array, SIGNATURE_PREFIX, ED25519_SIGNATURE_BYTES, loadPrivateKey, loadPublicKey, sign, decodeSignature, verifyWithPublicKey, verifySignature), errors.ts (AuditKeyError), keys.ts (KEY_ID_PREFIX, isKeyId, defaultKeyId, generateKeypair, derivePublicPem, createKeyRing / PublicKeyRing / KeyRingVerification, parsePublicKeysJson), index.ts; canonical/sha256.ts and its test moved here (policy/hash.ts, classify/cache.ts, llm/prompt.ts and rules/version.ts import ../audit/crypto.js now; the three classify purity tests only changed a comment). .env.example: ADGATE_PUBLIC_KEYS_JSON comment states the \n rule and points at parsePublicKeysJson. Tests: schemas 180 (+9: keys.test.ts), core 694 (+37: crypto.test 18 incl. the three moved sha256 vectors, keys.test 17, purity 4, index +1, sha256.test.ts's 3 removed).
Decisions: SIGNING RULE: the bytes signed are the UTF-8 encoding of the record_hash string exactly as it appears in the record, `sha256:` prefix included; never the raw digest and never the bare hex. Reason: docs/audit.md says "Ed25519 over record_hash" and a verifier only ever holds that string, so signing anything derived from it would need a second convention nobody can check. Signature = 'ed25519:' + standard base64 with padding of the 64 raw bytes (always 96 characters); decodeSignature re-encodes and compares so bad padding or stray bits count as malformed. verifySignature and ring.verify never throw (every malformed input is false). sign, generateKeypair, derivePublicPem, createKeyRing and parsePublicKeysJson THROW AuditKeyError (typed: reason + key_id, same role as PolicyValidationError) because they are boot/config-time code: load the signing key with loadPrivateKey and the ring with parsePublicKeysJson + createKeyRing at startup, never on the request path (S14 should accept a pre-validated private PEM, S15's verify() a PublicKeyRing). loadPublicKey checks the -----BEGIN PUBLIC KEY----- header before createPublicKey because node:crypto silently derives a public key from a private PEM; both loaders require asymmetricKeyType 'ed25519' because crypto.sign(null, ...) happily ECDSA-signs with a P-256 key. Error messages never contain key material (only key_id is echoed, and the Zod issue list names paths, not values). Default key_id is k_<YYYY>_<MM> in UTC from the injected clock (docs example k_2026_09); KEY_ID_PATTERN is one token that starts alphanumeric, up to 64 chars, no whitespace or '=' so `key_id=<id>` stays parseable; a custom keyId must match it. createKeyRing keeps a Map of KeyObject so verify() never parses PEM per request and a key_id like '__proto__' cannot reach a prototype; a non-string key_id is unknown_key_id. parsePublicKeysJson accepts '{}' (the .env.example default) so a fresh gateway boots; the gateway must then add its own signing key's public half (derivePublicPem) under ADGATE_SIGNING_KEY_ID or its own records verify as unknown_key_id. sha256Hex/sha256Prefixed accept Uint8Array as well as string (same digest for the same bytes) so a later content_hash over bytes needs no second helper; canonical/sha256.ts was deleted rather than shimmed so there is exactly one home. No dependencies added.
Gotchas: OpenSSL rejects a PEM whose line breaks were collapsed (one line, or spaces instead of newlines), so an env var must keep them as \n escapes inside the JSON string; PUBLIC_KEY_PEM_PATTERN mirrors that and rejects the flattened form early. Tampering a signature by overwriting one base64 character is not deterministic: the third-from-last character of a 64-byte signature is one of A/Q/g/w and already equals 'A' a quarter of the time, which made the first full run fail once; tamperSignature in crypto.fixture.ts flips a raw bit instead. A perl -0pi replacement whose text contains `${...}` or backticks is safer written as a heredoc rewrite of the file. Zod 4 renders z.record as propertyNames + additionalProperties: <value schema>, which json-schema.test.ts previously forbade for every non-strict schema. macOS cat has no -A; use od -c or tr to count backslashes.
Ideas: a createSigner({ key_id, private_pem }) that loads the key once for S14/S16 (sign() re-parses the PEM per call, roughly 50 microseconds); a PEM normaliser that restores line breaks for hosts that flatten env vars; the S16 keygen script should print the JSON entry to paste into ADGATE_PUBLIC_KEYS_JSON; cross-language signing vectors under fixtures/ once the Python SDK verifies records; a KeyRing.add() for hot rotation without a restart.

## 2026-09-02 S14 buildAuditRecord and hash chain
Built: packages/schemas/src/audit-record.ts (GENESIS_PREV_HASH, PrevHash, AuditSurface, AuditCreative, AuditDisclosure, AuditRecord, UnsignedAuditRecord, AuditRecordBody; AuditRecord registered in CONTRACT_SCHEMAS, 51 JSON files now; audit-record.test.ts parses the docs/audit.md example verbatim and pins the doc field order in the schema and the JSON file). packages/core/src/audit: privacy-hash.ts (conversationIdHash, userHash, normalizeSha256Hash), content-hash.ts (CREATIVE_CONTENT_FIELDS, creativeContent, creativeContentHash), chain.ts (GENESIS, AuditSigningKey, unsignedOf, recordHashInput, computeRecordHash, signRecord, nextPrevHash), record.ts (AuditAppContext, AuditPolicyContext, AuditPolicyResult, AuditMediation, BuildAuditRecordInput, emptyDemandTrace, buildAuditBody, buildAuditRecord), record.fixture.ts; everything re-exported from @adgate/core. Tests: schemas 192 (+12), core 775 (+81: privacy-hash 8, content-hash 5, chain 14, record 21, record-chain 32 with a two-record chain, a key rotation, a per-field tamper table whose key set must equal the unsigned record's keys, and evaluatePolicy output copied verbatim; purity file list; index +1).
Decisions: The docs example is copied verbatim except two prose values: user_hash "sha256:... or null" is written as "sha256:..." and signature "ed25519:base64..." as ed25519: plus a valid 88-character base64 body (dots are outside the alphabet); the elided sha256:... hashes and aud_01J.../cr_01J... ids parse as they are (S03 keeps those schemas prefix-only). override_rejected sits right after policy_decisions, defaults to [] in the schema, is always written explicitly by buildAuditRecord and is therefore part of the hashed content. record_hash is implemented literally: canonicalize(record minus record_hash and signature) + prev_hash, so prev_hash appears inside the canonical JSON and is appended once more; unsignedOf strips only those two fields and keeps unknown keys, so the record is hashed as given and an added column value breaks the hash (S15's verify should hash the loaded object, not a re-serialised parse). user_hash: an app-supplied 64-hex digest is normalised (sha256: prefix optional, hex lowercased) and never re-hashed, because the cap_state per-user key must be the same value on every turn; any other non-empty string is treated as a raw id the app sent by mistake and is salted and hashed like a conversation id; absent/empty is null. The salt must be a non-empty string (TypeError otherwise): an unsalted hash is a misconfiguration that must fail loudly, and the gateway validates it at app load. creativeContentHash covers exactly advertiser, advertiser_domain, headline, body, cta, url_template (what the user sees and where a click goes) and excludes id, pricing, targeting, status and the per-request Candidate fields, so the hash stays stable while the copy is unchanged and S20 can recompute it over the creatives row. signRecord sets key_id from the signing key (attestation and rotation re-sign under the current key) and discards a stale record_hash/signature, so S15's attest can pass the copied record straight in. buildAuditRecord decides fail-closed: a failed policy suppresses with its reason (a null reason becomes 'error') and never serves even if a candidate is handed in (the trace is still recorded); a passed policy serves only when mediation.selected is set, otherwise no_fill; mediation null (demand never ran) gives the empty trace, which is the acceptance shape for pre-demand suppressions. AuditPolicyResult and AuditMediation are structural interfaces inside audit/ (no import from policy/ or demand/), and PolicyEvaluation / MediationResult are assignable to them. Inputs are never mutated and never shared: fields are picked explicitly (Classification's six fields, surface without max_creatives, decisions, rejections) and the demand trace is structuredClone'd. buildAuditRecord throws only for inputs that break the types (empty conversation_id or salt: TypeError; bad key: AuditKeyError); the gateway's outer wrapper maps a throw to decision suppress reason error like everywhere else. No dependencies added.
Gotchas: The derived schemas (UnsignedAuditRecord, AuditRecordBody) are cut with .omit() from the unrefined object shape and the refinements are attached only to AuditRecord, so there is no question of whether zod 4 carries refinements through .omit(). A tamper table typed Record<keyof UnsignedAuditRecord, fn> plus an equality assertion against Object.keys(unsignedOf(record)) is the cheapest way to make a future schema field fail the tamper test until it is covered. The Prettier pass was clean on every new file except one; a perl -0pi replacement whose replacement text contains @adgate would interpolate as a Perl array in double quotes, so the Codebase-notes bullet was inserted with `sed -i '' 'Nr file'` from a file instead. `export const GENESIS = 'genesis' satisfies PrevHash` keeps the literal type. Core grew by exactly the new test count; the turbo build had to run once (schemas dist) before core's vitest saw the new schema exports.
Ideas: a createSigner({ key_id, private_pem }) that loads the KeyObject once (sign() still re-parses the PEM per record); cross-language vectors under fixtures/ (a committed throwaway keypair, one serve and one suppress input, the expected record_hash and signature) for the Python SDK's verify; for S18, record policy_hash = policyHash(mergeOverrides(...).policy) so the record pins the policy actually evaluated (the stored policy hash is derivable from the app) and derive the EvaluateResponse from the record (decision, reason, creative id, audit_id) so the API answer and the signed record can never disagree; a `region_blocked` detail note when user.region was absent (REVIEW-1 idea) now that override_rejected shows how per-request notes fit; AuditRecordBody could gain a `latency_ms` if the dashboard wants it, but that would change the hashed content and needs a human decision.

## 2026-09-02 S15 attest() and verify()
Built: packages/core/src/audit: attest.ts (AuditAttestError with reason invalid_record | already_attested | invalid_model_output_hash | invalid_attested_at | invalid_prev_hash, AttestOptions { prev_hash, signing }, isAttested, isUnattested, attest(record, model_output_hash, now, opts)), verify-checks.ts (VerifyContext, PreviousRecord, VerifyRole, VERIFY_DETAIL, one function per documented check; internal, not on the package index), verify.ts (verify(record: unknown, keyRing: PublicKeyRing, ctx?) -> VerifyResponse, VERIFY_CHECK_ORDER, re-exports VERIFY_DETAIL and the ctx types); describeIssues moved from keys.ts into errors.ts and shared; audit/index.ts exports attest.ts and verify.ts. Tests: attest.test.ts 15, verify.test.ts 23 (tamper matrix, rotation, attest round trip), verify-checks.test.ts 16 (chain incl. pruned, creative_hash, disclosure, separation, supersedes), verify-malformed.test.ts 28 (never throws, check order, docs/api.md example), verify.fixture.ts; purity file list now eleven files; index +1. Core 858 (+83), schemas 192.
Decisions: ATTESTATION: the attested record keeps the original's id (the audit_id the SDK holds; the newest version per id is what the API serves) and takes its own chain position: prev_hash = opts.prev_hash = nextPrevHash(the app's latest record at attestation time), supersedes_hash = original.record_hash, attested_at = now, key_id from opts.signing (rotation-safe), record_hash and signature recomputed; S19 stores both rows keyed by record_hash and marks the latest version per id. attest validates with AuditRecord.safeParse and copies from the PARSED data (unknown keys dropped, override_rejected defaulted, nothing shared with the input) rather than unsignedOf(raw) as S14 sketched, so an attestation is always exactly the schema shape. Suppress records are attested too: the SDK attests every turn and the record then proves separation even though nothing rendered. Any attestation field already set is 'already_attested' (double attestation rejected; map to 409). model_output_hash must parse as Sha256Hash, now as IsoTimestamp, prev_hash as PrevHash and never 'genesis' (an attestation is never the first record of its app). An unusable signing key still surfaces as AuditKeyError (config error). VERIFY: takes unknown (the object as loaded) and an S13 PublicKeyRing built at boot (never a raw key map, so nothing can throw at verify time); checks come in exactly the VerifyCheckName order; schema failure reports every later check ok:false 'skipped: schema invalid'; record_hash is recomputed over the RAW object (an added column value fails it; a Date-valued extra key gives 'error: TypeError'); every other check reads the parsed record; each check runs under a guard (a throw = ok:false 'error: <Error.name>') plus an outer guard, and a garbage ctx counts as empty. Semantics and details (VERIFY_DETAIL): chain: prev_hash genesis -> ok 'genesis' only when ctx.prevRecord is null/undefined (a supplied record is 'genesis record given a previous record'); ctx.prevRecord === 'pruned' -> ok 'pruned' (S37 passes it when retention deleted the predecessor) [REVIEW-2: now { pruned: true, retain_cutoff }, ok only when record.ts >= retain_cutoff, and prevRecord is the positional predecessor]; missing -> 'previous record missing'; else prevRecord.record_hash must equal prev_hash and app_id must match. signature: the ring's verdict verbatim (key_id=<id> / unknown_key_id / bad_signature). creative_hash: creative null -> ok 'not applicable'; ctx.storedCreativeHash required (S20: creativeContentHash over the creatives row), exact string compare. disclosure_present: label non-blank after trim, position after_answer, style separate_block (only a blank label is reachable past the schema). separation_attested: fully attested -> ok; partial -> 'attestation incomplete'; unattested suppress -> ok 'not applicable' (S20); unattested serve -> 'not attested', so an unattested serve record is valid:false (docs: true only if attested); a record reached recursively through supersedes_hash is verified with role 'superseded' and reports ok 'superseded' when unattested, because the superseding record carries the proof [REVIEW-2: the role is gone; ctx.supersededBy = the attesting record gives ok 'attested by superseding record', set automatically for the nested check]. supersedes: null -> ok 'not applicable'; else ctx.supersededRecord must exist with record_hash === supersedes_hash, the same id and app_id, and must not itself be an attestation ('superseded record is itself an attestation': attest never re-attests, and this bounds the recursion to one level without a depth counter); ctx.verifySuperseded === false -> ok 'superseded record present, not verified'; otherwise the recursive verify under ctx.superseded (its own prevRecord and storedCreativeHash) must be valid -> ok 'superseded record verified', else 'superseded record invalid: <failing check names>'. Acceptance "its superseded record still verifies" is read as: the original stays intact and every integrity check passes on it alone; a serve original reports only separation_attested 'not attested' (a suppress original is fully valid) [REVIEW-2: the AC "its superseded record still verifies" now holds in full when the gateway passes ctx.supersededBy = the attesting record, which S20 must do]. ok checks carry no detail except signature, chain genesis/pruned, the not-applicable cases and the supersedes verdict, matching the docs/api.md example shape. No dependencies added.
Gotchas: A rest-destructuring discard ({ a: _a, ...rest }) trips no-unused-vars (the S09 gotcha again); filter Object.entries instead. verify-checks.ts imports isAttested/isUnattested from attest.ts and verify.ts imports both, so the check module cannot import verify.ts: the recursive call is injected as a callback. A revoked Proxy as ctx makes Array.isArray throw, the only way found to exercise the outer guard. attest() refuses double attestation, so the forged chain-of-attestations test strips the attestation fields of an attested record by hand while keeping its record_hash. Prettier re-wrapped five of the new files; nothing crossed 300 lines (verify.test.ts is 296).
Ideas: the supersedes check could also compare the superseded body with the attested body minus the attestation fields, so a signed attestation that changes the creative or classification fails verification even with a valid signature; a verifyChain(records[]) helper for S20's advertiser report and the S37 retention job; carry the nested VerifyResponse of the superseded record in an extra field if the dashboard wants it; a cross-language fixture (record, attested record, ring JSON, expected checks) for the Python SDK's offline verify; S19 could call verify() with verifySuperseded false before attest to refuse attesting a record whose stored hash no longer recomputes.

## 2026-09-02 S16 Gateway skeleton, config, Drizzle schema, migrations
Built: packages/gateway: config.ts (GatewayEnv/GatewayConfig Zod schemas, loadConfig, ConfigError with one readable line per variable and hints), signing.ts (loadSigningKeys -> signing key + PublicKeyRing), logger.ts (pino with deep redaction of messages/context_summary/authorization and friends), request-id.ts (X-Request-Id echo or ULID, child logger, one request log line), app-env.ts, app.ts (createApp: cors, GET /healthz, JSON 404/500 in the docs/api.md error shape, HTTPException passthrough), env-file.ts (repo-root .env via process.loadEnvFile), cli.ts, keygen.ts (`pnpm --filter @adgate/gateway keygen` prints the three env lines), server.ts (boot, serve, graceful shutdown), db/tables/{apps,catalog,audit,caps,columns}.ts, db/schema.ts, db/client.ts (createDb), db/migrate.ts (runMigrations + CLI), db/test-support.ts, drizzle.config.ts and the committed initial migration drizzle/0000_init.sql + meta. vitest.config.ts runs files serially with a setup file. Tests: 48 in the gateway (config 13, logger 5, request ids 5, app 5, signing 6, keygen 3, schema 4, index 2, integration 5: migrations create every table and the required indexes, migrations idempotent, GET /healthz against the test database). .env.example gained NODE_ENV, LOG_LEVEL, KOAH_ENABLED/KOAH_API_KEY/KOAH_BASE_URL, GRAVITY_ENABLED/GRAVITY_API_KEY/GRAVITY_BASE_URL and a sharper ADGATE_SIGNING_KEY_PEM comment; .prettierignore ignores packages/gateway/drizzle/. `pnpm db:migrate` run against the docker Postgres on 5433: `\dt` lists the 10 tables and pg_indexes has audit_records_app_id_ts_idx, events_audit_id_idx, cap_state_pkey (app_id, conversation_hash) and classify_cache_expires_at_idx; a second run is a no-op. Both `tsx src/server.ts` (pnpm dev) and `node dist/server.js` (pnpm start) booted with a keygen key, answered /healthz with { ok: true }, echoed X-Request-Id, returned the JSON 404 body and the CORS header, and exited 0 on SIGTERM; an empty or garbage ADGATE_SIGNING_KEY_PEM exits 1 with the variable named.
Decisions: Tests drive the app with Hono's app.request() instead of supertest (no port, no socket; supertest stays available in the stack if a story needs a real socket). No dotenv: Node's process.loadEnvFile loads <repo root>/.env (found by walking up to pnpm-workspace.yaml so cwd does not matter), environment variables already set always win, and CI has no .env. GET /healthz does not touch Postgres (liveness; a readiness probe is an Idea). createApp takes `db` as an optional dependency so unit tests run without Postgres; server.ts always passes it. The keygen output is designed for Node's .env parser: the PEM is double-quoted with \n escapes (expanded by the parser) and the JSON is an unquoted value kept verbatim; config.ts additionally unescapes literal \n in the PEM for hosts that flatten env vars. loadSigningKeys treats a JSON entry for the signing key id that is not its own public half as a config error rather than silently overriding it. The redaction is a deep key-name walk in pino's formatters.log rather than a redact path list, because the request body shapes nest messages at several depths and a path list cannot follow `*.*`. HTTPException is the one way routes and middleware reject: its status maps to a stable code (errorCode) and its res headers are copied, so S17's auth (401) and rate limit (429 + Retry-After) need no new error plumbing. Schema choices beyond the story text: cap_state.turn_count (evaluate calls seen) so turns_since_last is derivable without parsing turn_id strings; audit_records index (advertiser_id, ts) and events index (app_id, ts) for the advertiser report and dashboard; CHECK constraints on api_keys.role (+ advertiser_id iff advertiser_read), creatives.source, creatives.ecpm >= 0, audit_records.decision and events.type; no ON DELETE CASCADE anywhere (apps with data cannot be deleted by accident). drizzle-kit reads src/db/tables/*.ts directly (drizzle.config.ts), and the table modules import @adgate/schemas as types only with local CHECK tuples pinned to the contract enums by schema.test.ts. Dependencies added to @adgate/gateway (all on the CLAUDE.md stack list): hono ^4.13.5, @hono/node-server ^2.1.1 (peer hono ^4), drizzle-orm ^0.45.2, postgres ^3.4.9, pino ^10.3.1, zod ^4.5.4, plus @adgate/core and @adgate/schemas workspace:*; dev: drizzle-kit ^0.31.10 (declared in the gateway package, not the root, because only this package uses it). pino-pretty was not added (JSON lines are fine for now; see Ideas).
Gotchas: drizzle-kit loads schema files through a CommonJS require hook, which fails on @adgate/schemas (exports map has only an `import` condition: ERR_PACKAGE_PATH_NOT_EXPORTED); hence the type-only import rule for table modules. Zod 4's `.options` is typed as `T[]`, not a tuple, so drizzle's `text(name, { enum })` needs an `as const` tuple (`satisfies readonly Decision[]` keeps it tied to the contract type). pino's stdSerializers/stdTimeFunctions live on the default export in the type definitions; the named `pino` import lacks them. The fetch Headers API rejects a newline inside a header value, so the malformed X-Request-Id test uses a slash and an '=' instead. A background job in a non-interactive bash script inherits SIGINT ignored and Node then never fires its SIGINT listener, so scripted smoke tests must send SIGTERM (Ctrl-C in a terminal works); the first smoke run also reused one log file for two servers, which interleaved their output. Run prettier --check from the repo root: inside packages/gateway it does not see the root .prettierignore and flags dist/. drizzle-kit generate writes meta/_journal.json even when it fails; delete the folder before retrying. tsup keeps the directory of path entries (dist/db/migrate.js), so findMigrationsDir checks both ../../drizzle and ../drizzle relative to the module. The scratch keygen key used for the smoke tests lives outside the repo and .env was not modified (its ADGATE_SIGNING_KEY_PEM is still empty; run keygen before `pnpm dev`).
Ideas: LOG_PRETTY=true with pino-pretty as a dev-only transport for `pnpm dev`; a GET /readyz that pings Postgres for orchestrators while /healthz stays a liveness probe; DB_POOL_MAX env var; hono secure-headers and a body size limit middleware on /v1/evaluate (S17/S18); serve /openapi.json from CONTRACT_SCHEMAS; a createSigner in core so sign() stops re-parsing the PEM per record; an `apps.deleted_at` soft delete instead of cascades; a retention job (S37) index on raw_text(created_at) and audit_records(ts) alone; record `verification_key_ids` in a /v1/keys endpoint so verifiers can fetch public keys.

## 2026-09-02 REVIEW-2 Fixes from the S09-S15 code review
Built: Applied findings 1-15 of the review of be979f8..9d85ea3, each with a failing test first; 16 is a recorded decision (no code or doc change). (1) verify chain: ctx.prevRecord is the POSITIONAL predecessor (the app's record immediately before this one), JSDoc and tests say so; a fork whose prev_hash points at an older record than the positional predecessor fails 'prev_hash does not match the previous record' and a re-signed record claiming genesis in the middle of a chain fails 'genesis record given a previous record'; the Codebase notes carry the audit_records.seq obligation for S18. (2) select.ts owns mediationScore and compareByRevenue (moved from mediate.ts, re-exported from @adgate/core under the same names) and rankCandidates sorts with it, so DirectAdapter and AffiliateAdapter cut their top 3 in the revenue order mediation ranks with (a creative with match 0.8 at ecpm 100 beats match 1 at ecpm 10; four exact matches at ecpm 10 no longer push a wildcard match at ecpm 100 out of the top 3); the S09 c001 expectation still holds (DB Cloud 0.85 * 12 first, Deploy second). compareCandidates is deleted. (3) verify: the internal role mechanism is replaced by ctx.supersededBy (the attesting record); an unattested record it supersedes reports separation_attested ok 'attested by superseding record'; without it a serve record is 'not attested' (valid false) and a suppress record 'not applicable'; verify-supersedes.ts passes the attesting record as supersededBy to the nested verification automatically. (4) The nested verification inherits storedCreativeHash when ctx.superseded.storedCreativeHash is undefined and ctx.superseded.prevRecord is documented as the original's own positional predecessor (required unless the original is genesis); exactly { prevRecord, storedCreativeHash, supersededRecord, superseded: { prevRecord } } verifies. (5) normalizeDomain strips scheme, userinfo, port, path/query/fragment, a leading *. or . and trailing dots; *.competitor.com, .competitor.com and https://competitor.com/ each exclude competitor.com and shop.competitor.com and never notcompetitor.com. (6) mediate's last-resort catch writes one { source, candidates: 0, latency_ms: 0, error: 'adapter_error:<name>' } per requested source (a policy whose competitor_exclusions getter throws Error('...secret') yields 'adapter_error:Error'). (7) describeError returns the Error name only ('NonError' otherwise), mediate reuses it instead of its own copy, and a DirectAdapter whose catalog row throws Error('secret message') puts exactly 'Error' in the trace. (8) buildAffiliateUrl ignores program_id for amazon (the owner's tag is the only tag a link may credit) and setTagParam drops TAG=, Tag=, %74ag= and %54%41%47= spellings (decoded, lower-cased names; an undecodable name is kept). (9) Sha256Hash = /^sha256:[0-9a-f]{64}$/ (SHA256_HASH_PATTERN exported); AttestRequest, AuditRecord and Sha256Hash JSON files regenerated; every fixture and test that used a short fake hash now uses a real-format one; attest rejects 'sha256:abc' and uppercase hex through the schema. (10) The supersedes check additionally requires the attestation to be a faithful copy of the superseded record with model_output_hash, separation_attestation, attested_at, supersedes_hash, prev_hash, key_id, record_hash and signature removed (ATTESTATION_FIELDS in verify-supersedes.ts, canonical JSON of the parsed records compared), detail 'body_mismatch'; a re-signed attestation that swaps the creative passes record_hash and signature and fails only supersedes, and the tamper matrix now expects supersedes to fail on every body edit of an attestation. (11) The bare 'pruned' sentinel is replaced by prevRecord: { pruned: true, retain_cutoff }; chain is ok 'pruned' only when prev_hash is not genesis and record.ts >= retain_cutoff, otherwise 'record predates the retention cutoff of its pruned predecessor' (a missing or unparseable cutoff fails too; the old string is 'previous record missing'). (12) ULID_PATTERN = /^[0-7][0-9A-HJKMNP-TV-Z]{25}$/, so isUlid and ulidTime reject a first character above 7. (13) packages/schemas/src/doc-examples.fixture.ts extracts the ```json blocks of docs/audit.md and docs/api.md at test time, keyed by the ## heading above each block, and fills only the ... placeholders (EXAMPLE_SHA256, EXAMPLE_SIGNATURE); audit-record.test.ts and api-examples.test.ts (evaluate request/response, attest, events, verify) read the docs instead of hand copies, typed with z.input<typeof Schema> so evaluate.test.ts can keep spreading them. (14) AuditPolicyResult = Pick<PolicyEvaluation, 'allowed' | 'reason' | 'decisions'> and AuditMediation = Pick<MediationResult, 'selected' | 'trace'> (type-only imports; the audit purity graph is unchanged). (15) canonicalize's JSDoc and a new `notes` array in fixtures/canonical-json.json define key order as UTF-16 code unit order (not code point order; Python must sort by key.encode('utf-16-be')), and a vector with an emoji key next to U+FB01 pins it ({"z":0,"😀":1,"ﬁ":2}). Files: verify-checks.ts split into verify-checks.ts + verify-supersedes.ts (audit purity list updated); tests split into verify-checks.test.ts, verify-supersedes.test.ts, verify.test.ts and verify-tamper.test.ts to stay under 300 lines. Tests: schemas 194 (+2), core 890 (+32), gateway 48 (unchanged; it compiles against the new types without edits).
Decisions: (16) verify returns the 8 checks docs/audit.md lists; the docs/api.md example (7 checks, no supersedes) is illustrative; a human may align api.md later. (9) Tightening Sha256Hash to sha256: plus 64 lowercase hex digits enforces the documented format and does not change the contract's meaning; PrevHash and every hash field follow, and the JSON schema files now carry the pattern instead of minLength 8 + ^sha256:.*. (1) The gateway must persist a per-app chain position: S18's migration adds an integer audit_records.seq (UNIQUE (app_id, seq); attested rows take their own next seq at insert time; nextPrevHash reads the highest seq), and /v1/verify loads prevRecord as the row at seq - 1 of the same app, or { pruned: true, retain_cutoff } when S37 deleted it. Loading the predecessor by record_hash === prev_hash would accept forks and is now documented as wrong. (3) supersededBy counts only when it is a complete attestation (separation_attestation true, model_output_hash and attested_at set) whose supersedes_hash, id and app_id name this exact record; anything else proves nothing and the record stays 'not attested'. An unattested suppress record with a valid supersededBy also reports 'attested by superseding record' (checked before 'not applicable'). (4) storedCreativeHash is inherited only when the nested value is undefined; an explicit null stays null (the gateway looked and found no row). (10) The body comparison parses the superseded record first (one that does not parse is 'superseded record invalid: schema' without recursing) and runs even with verifySuperseded false, because identity includes the body; the check order is missing, hash, id, app, is-attestation, schema, body, then the recursion. (2) compareCandidates (targeting-first) is deleted rather than kept as an alias so there is exactly one ordering in the package. (7) describeError carries no message anywhere, so an adapter-level throw reads 'TypeError' in the trace rather than 'TypeError: <message>'; the network stubs inherit this. (8) The Candidate object still carries the catalog row's program_id and url_template (Candidate spreads CatalogCreative); only resolved_url is guaranteed to credit the owner's tag. (11) The PrunedPredecessor shape is { pruned: true, retain_cutoff: IsoTimestamp }; retain_cutoff is compared with Date.parse, so any timestamp Date understands works, and NaN fails closed. (13) doc-examples.fixture.ts keys examples by heading so a reordered doc still maps and a changed block count fails naming the heading; the two hand copies left in core (the docs/audit.md demand block in mediate.test.ts, the docs/api.md verify example in verify-malformed.test.ts) stay, since core tests reading docs across the workspace buys little. (15) The fixture writes the emoji key both as \ud83d\ude00 escapes and as the raw character so a parser bug in either form shows. No dependencies added; docs/api.md, docs/policy.md, docs/audit.md untouched.
Gotchas: Candidate spreads the whole catalog row, so asserting that a DemandResponse's JSON never contains a creative's program_id is wrong; assert on resolved_url. The audit purity test's 300-line rule covers test files too: the added cases forced two test splits (verify-supersedes.test.ts, verify-tamper.test.ts) before the suite went green. network-stub.test.ts had encoded the old '<Name>: <message>' adapter error format. Zod 4 renders .regex() as "pattern" and drops the previous minLength/startsWith pair, which is why three JSON schema files changed. A z.input<typeof Schema> cast is what keeps the doc-extracted examples typed for the tests that destructure and spread them; the docs' prefix-only ids (aud_01J...) still parse, only the sha256:... and ed25519:base64... placeholders need filling. An object literal { pruned: true } widens to boolean and no longer satisfies PrunedPredecessor: annotate the fixture. A python re pattern built as start.*?end with start === end demands the anchor twice (an editing-script slip, not a repo issue).
Ideas: a verifyChain(records[]) helper walking seq order for S20's advertiser report and the S37 retention job; expose retain_cutoff in the /v1/verify chain detail so an auditor sees why a predecessor is missing; validate third-party DemandResponses with safeParse at the gateway boundary; a destination_url per creative; a Python SDK test that replays fixtures/canonical-json.json sorting by utf-16-be and one that replays the docs examples through the same extraction; a seq gap check (strictly increasing without holes per app) as a cheap chain integrity probe in the dashboard.

## 2026-09-02 S17 API keys, auth middleware, app and creative seeding
Built: packages/gateway/src/auth/keys.ts (API_KEY_PATTERN, generateApiKey, parseApiKey, parseBearerHeader, hashApiSecret, verifyApiSecret, isArgon2idHash, burnVerifyTime, ARGON2_OPTIONS), auth/repository.ts (ApiKeyStore, createApiKeyStore, issueApiKey, revokeApiKey), auth/middleware.ts (bearerAuth, UNAUTHORIZED_MESSAGE, WWW_AUTHENTICATE, LAST_USED_THROTTLE_MS), app-env.ts (AuthContext; AppVariables gains auth and app), apps/register-app.ts (registerApp, defaultPolicyYaml, APP_ID_PREFIX), catalog/seed.ts (seedCreatives, ADVERTISER_ID_PREFIX), scripts/{args,run,create-app,create-key,seed-creatives}.ts with package.json scripts create-app / create-key / seed-creatives and tsup entries; AppRow / ApiKeyRow / AdvertiserRow types in db/tables/apps.ts and DbOrTx in db/client.ts; everything except the scripts re-exported from the index. Tests: gateway 97 (+49): keys 11 (format, uniqueness, injected randomness, parsing, argon2id hash and verify, corrupt hashes verify false, decoy verify), middleware unit 6 with a scripted store (no roles = TypeError, touch failure still 200, throttle, missing app = 401, store error = 401 without the secret in logs, malformed header never hits the store), middleware integration 14 against api_keys (401 body for no header/other scheme/bare key/malformed/unknown prefix/wrong secret/revoked with WWW-Authenticate, 200 with the auth and app context, 403 both directions, either-role route, last_used_at written, no secret/presented key/hash in debug-level logs while key_id is), register-app 5 (defaults hash = loadPolicyFromYaml(stored YAML) = policyHash(PolicyConfig.parse({ app_id })), examples/policy.example.yaml stored verbatim with its hash, invalid policy writes nothing, blank name, duplicate names make two apps), seed 6 (3 advertisers + 3 creatives with cr_/adv_ ids and content hashes equal to creativeContentHash, second run 0 inserted / 3 unchanged with identical rows, a changed entry updates in place keeping its id, network/program_id kept, --app catalog separate from the global one, unknown app or invalid entry writes nothing), args 7. Smoke against adgate_test via DATABASE_URL: create-app with the defaults and with examples/policy.example.yaml, seed-creatives twice (3/3 inserted, then 3/3 unchanged) and with --app (3 private rows, advertisers unchanged), create-key for both roles and the usage error for a missing --advertiser; `node dist/scripts/<name>.js --help` after pnpm build. pnpm test / typecheck / lint / prettier --check all green; docs untouched; no new env vars; CI unchanged.
Decisions: The function is registerApp, not createApp, because createApp (the Hono factory) already lives on the package index. Scripts live in src/scripts/ because src/cli.ts (helpers) already exists and a cli/ directory next to it would be confusing. Argon2id at the OWASP minimum (19 MiB, t=2, p=1): API secrets carry 256 random bits, so the hash only defends against a database leak, and every /v1 request pays one verify (about 15 ms here, on the libuv pool, off the event loop); the parameters are encoded in each hash so they can change later without re-issuing keys. Constant time: argon2.verify is the only secret comparison (constant time by construction); no plain === comparison of secret material exists, so crypto.timingSafeEqual has no call site; unknown prefixes run a decoy verify, verification runs before the revoked check, and every failure shares one 401 body and header, so timing and bodies reveal neither existing prefixes nor revocations. The app row is loaded for both roles so S20's audit/verify routes can rely on c.get('app') whatever the key role. The seed natural key is (advertiser, headline) inside one catalog (global or one app's): seeding with --app after a global seed adds private rows rather than moving the global ones. registerApp stores the YAML exactly as given (an operator file keeps its own app_id, e.g. example-chat) and generates a three-line default document otherwise; the stored YAML is what hashes, and both paths go through loadPolicyFromYaml. last_used_at is throttled to one write per key per 60 s (row value read at auth time) to keep evaluate from writing api_keys on every call. The key prefix alphabet excludes `_` so parsing never needs a fixed length. parseFlags ignores a bare `--` because pnpm 9 forwards it to the script; usage lines show the form without it. Dependency added: argon2 ^0.45.1 (on the CLAUDE.md stack list; native addon; pnpm 9 ran its install script without an onlyBuiltDependencies entry, so the root package.json is unchanged).
Gotchas: argon2 0.45 writes the PHC parameters alphabetically (`m=19456,p=1,t=2`); the first isArgon2idHash pattern demanded m,t,p and every verify short-circuited to false, which surfaced as 401 on every valid key. pnpm 9 forwards a literal `--` to the script, so `pnpm create-app -- --help` reached the parser as ['--', '--help'] and exited 1 until the marker was ignored. A multi-line perl -0pi substitution with `|` delimiters prepended its replacement to the top of args.ts instead of splicing it; rewrite whole files with a heredoc. A test route handler mounted after createApp is typed with `Context<AppEnv>` from 'hono'; Parameters<typeof app.get> does not resolve. The DB smoke and the vitest suite share adgate_test and both truncate/write it: never run them concurrently. argon2 compiled from source on this Mac (node-gyp, about 7 s, needs the Xcode command line tools) even though prebuilds/darwin-arm64 ships; CI on ubuntu-latest has the toolchain and the linux-x64 prebuild.
Ideas: an in-memory verified-key cache (key_prefix -> sha256(secret) with a short TTL) so hot keys skip argon2 once evaluate QPS matters; revoke-key and list-keys scripts; create-app --affiliate <json> to set apps.affiliate_config; argon2.needsRehash on a successful verify to upgrade old hashes when ARGON2_OPTIONS change; a UNIQUE index on creatives (advertiser_id, headline, coalesce(app_id, '')) to make the seed's natural key a database guarantee; per-key rate limiting keyed on auth.key_id (docs/api.md 429 + Retry-After); a --json flag on the scripts for automation.

## 2026-09-02 S18 POST /v1/evaluate end to end
Built: packages/gateway/src/evaluate: deps.ts (EvaluateDeps, EvaluateDepsOverrides, createEvaluateDeps, llmFromConfig), route.ts (evaluateRoute, parseEvaluateBody, INVALID_REQUEST_CODE, APP_MISMATCH_MESSAGE), pipeline.ts (evaluate(deps, ctx) never rejects; EvaluateContext, EvaluateResult, EvaluateDiagnostics, AUDIT_ID_PREFIX), policy-loader.ts (createPolicyLoader), caps.ts (utcDay, capStateFrom, createCapReader, applyCapUpdates), classify-cache-pg.ts (createPgClassifyCache, createLayeredClassifyCache, CacheSource), adapters.ts (loadCatalog, affiliateConfigOf, createAdapters, createAdapterFactory), audit-store.ts (createAuditStore, readLatestAudit), response.ts (clickUrl, toCreative, buildEvaluateResponse, errorEvaluateResponse), test-support.ts; src/http-error.ts (errorCode/errorResponse moved out of app.ts and re-exported); app.ts mounts POST /v1/evaluate behind bearerAuth({ roles: ['app'] }) when AppDeps.evaluate is given; server.ts wires createEvaluateDeps(config, db, { signing }); index.ts exports the evaluate modules. Migration drizzle/0001_audit_seq.sql (+ meta/0001_snapshot.json): audit_records.seq integer NOT NULL and UNIQUE (app_id, seq); applied to the dev database with pnpm db:migrate (column and index verified with psql). Core: ClassifyOutcome.keywords (keywordsFromRulesMatches over the rules matches; the rules stage now runs before the cache lookup so a hit carries keywords too) with classify-keywords.test.ts. Tests: gateway 149 (+52: serve 5 incl. affiliate through an owner-configured network and the evaluate log line, suppress 10 covering every documented reason plus per_user_per_day and an empty catalog, chain/overrides 6, errors/LLM/fail-closed 9, privacy 3, classify cache pg 5, caps 3, response 6, policy loader 4, seq index and index exports), core 894 (+4), schemas 194. Smoke over a real socket (tsx src/server.ts on :8799 against the dev DB with a throwaway signing key exported into the process environment): /healthz, serve in 55 ms cold with the /c/<audit_id> URL, frequency_cap on the second call, 400 invalid_request on bad JSON, 401 without a key, seq 1 (genesis) and 2 in the dev audit_records, clean SIGTERM shutdown. pnpm build / test / typecheck / lint / format:check all green. No dependencies added, no env vars added, docs untouched.
Decisions: A body that is not JSON or fails EvaluateRequest is 400 { error: { code: 'invalid_request' } } and a body whose app_id is not the key's is 403: client errors decided before the pipeline, consistent with docs/api.md error behaviour (evaluate never returns 5xx; these are 4xx); everything after validation is wrapped and answers 200. The record pins policy_hash = policyHash(effective policy) after mergeOverrides (equal to the stored hash when no override was accepted, different when one was) and policy_version = apps.policy_version; override_rejected = mergeOverrides().rejected. ClassifyOutcome gained keywords rather than exposing the rules matches, and the rules run before the cache lookup so cached and uncached turns produce identical demand requests. The Postgres cache tier is consulted before classify() and written through after it (classify's ClassifyCache is synchronous); the upsert runs concurrently with mediation and the audit transaction and is awaited (flush) before responding, so a test can assert the row; every cache error is a warn and a miss. Chain appends are serialised per app with SELECT ... FOR UPDATE on the apps row inside the audit transaction; seq = highest + 1; cap_state, user_day_caps and raw_text are written in the same transaction, so a rolled-back record leaves no counted turn behind. Cap state is read before the transaction (a concurrent pair of calls in one conversation could both serve under per_session 1; see Ideas). The response is built and validated from buildAuditBody BEFORE persisting (a contract failure fails closed without a serve record) and derived from the persisted record afterwards. The error path writes its own record (zeroed classification, reason error, empty decisions, empty demand trace, stored policy_hash, disclosure from the policy or the documented defaults) and still counts the turn; if persisting fails too, a fresh aud_ id is returned and the log says the record does not exist (the response schema requires an audit_id). Errors are logged by name only (error_name), never message, because driver messages can quote values. Reason mapping by rules alone: l011 ('help me write a birthday message for my mom', confidence 0.75, intent 0) gives low_commercial_intent and l014 ('tell me a joke about compilers', confidence 0.6) gives low_confidence, because min_confidence (rule 4) runs before min_commercial_intent (rule 5); the merged path's low_confidence is covered with a FakeLlmClassifier at confidence 0.3. blocked_categories overrides are unioned, never rejected (docs: may add), so 'removing health' is silently ignored and the test asserts health still blocks while allow_paid_tiers, serve_to_tiers, frequency_caps.per_session, min_confidence and privacy.store_raw_text appear in override_rejected. raw_text stores the prepared 'role: content' text (last 4 user/assistant messages, 4,000 code points), the same string that was hashed and classified. apps.affiliate_config is parsed with AffiliateConfig.parse per request (invalid JSON fails closed as error; it is validated at write time); the catalog is loaded per request and rows the schema rejects are skipped with a warning naming the creative id. Unattested serve records verify with every check ok except separation_attested ('not attested', S15 semantics); the tests assert exactly that.
Gotchas: TRUNCATE creatives, advertisers CASCADE also empties api_keys (advertiser_id FK) and every later request answers 401; deactivate creatives instead. Hono's app.request() returns Response | Promise<Response>; wrap the helper in an async function. VerifyContext is exported by @adgate/core, not @adgate/schemas. The gateway resolves @adgate/core from dist, so after a core change run pnpm --filter @adgate/core build before tsc in the gateway. psql -c with several statements prints only the last result set. The repo .env still has an empty ADGATE_SIGNING_KEY_PEM (S16 note): pnpm dev needs keygen first; the smoke exported a generated key into the process environment only, .env untouched. The bearerAuth child logger already carries app_id and key_id, so passing app_id again produced a duplicate JSON key in the evaluate line. A rest-destructuring discard ({ surface: _surface, ...rest }) trips no-unused-vars (S09/S15 gotcha again); spread { ...body, surface: undefined } instead. A tsx/node -e script outside packages/* cannot resolve @adgate/* (exports map): import the built dist by absolute path for quick probes.
Ideas: read cap state inside the audit transaction (or lock the cap_state row) so two concurrent calls in one conversation cannot both serve under per_session 1; a body size limit and per-key rate limiting (429 + Retry-After) on /v1/evaluate; a DEMAND_TIMEOUT_MS env var; a short-TTL in-memory catalog per app instead of a query per request; counters per classify_source / cache_source / reason for the metrics story; GET /readyz; S19 should extend createAuditStore with a supersede operation (same lock, next seq, flip is_latest) rather than a second transaction; a region_blocked detail note when user.region was absent; expose ClassifyOutcome.source in the dashboard's "why suppressed" view.
