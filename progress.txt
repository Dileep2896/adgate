# adgate progress log
# One entry per completed story. Newest at the bottom. Format:
#
# ## <YYYY-MM-DD> <story id> <title>
# Built: one or two lines
# Decisions: choices the next iteration must respect
# Gotchas: anything that cost time
# Ideas: out of scope improvements (do not implement)
#
# If blocked: a single line "NEEDS HUMAN: <story id> <reason>" and stop.

## Codebase notes (maintained across iterations, edit in place)
- Docker Postgres host port is ${PGPORT_HOST:-5432} in docker-compose.yml. On this dev machine a native Postgres owns 5432, so .env sets PGPORT_HOST=5433 and both DATABASE_URL values use 5433. CI and fresh clones keep 5432. Always read the port from env, never hardcode 5432.
- CI is .github/workflows/ci.yml: ubuntu-latest, Node 20, pnpm from package.json packageManager (pnpm/action-setup@v4 with no version input), `pnpm install --frozen-lockfile`, then typecheck, lint, test. A postgres:16 service container (user/password/db adgate) listens on localhost:5432 and the job env exports DATABASE_URL and DATABASE_URL_TEST on 5432. Service containers cannot mount scripts/db/init.sql, so a psql step creates adgate_test (idempotent). Integration tests must read DATABASE_URL_TEST from the environment and truncate tables; they must not assume the compose init script ran.
- .env.example is the canonical list of env vars, one comment line per variable directly above it. When a story adds a variable, add it there with a comment (and to the CI job env if tests need it).
- LICENSE is the canonical Apache 2.0 text from apache.org (11358 bytes). Never edit it; add third-party notices in a separate NOTICE file if ever needed.
- @adgate/schemas (zod 4) is the API contract: import schemas and their z.infer types from '@adgate/schemas' (named exports, one module per responsibility under packages/schemas/src). CONTRACT_SCHEMAS lists every schema that has a JSON Schema file in packages/schemas/json/<Name>.schema.json; regenerate with `pnpm --filter @adgate/schemas gen:json` (json-schema.test.ts fails when a file is stale, and the directory is prettier-ignored). SENSITIVE_TAXONOMY and CATEGORIES_TAXONOMY live in packages/schemas/src/taxonomy.ts and must equal fixtures/classify-fixtures.json (taxonomy.test.ts enforces it); Classification.categories and .sensitive are validated against them. tsx is a root devDependency for running TypeScript dev scripts (`tsx src/foo.ts`) without a build.
- Policy (docs/policy.md): PolicyConfig, PolicyOverrides (strict deep partial, now the type of EvaluateRequest.policy_overrides), OverrideRejection and the leaf schemas live in packages/schemas/src/policy*.ts; parsePolicy(unknown) and parsePolicyYaml(text) throw PolicyValidationError (config-time only, never on the request path). @adgate/core owns the pure helpers: canonicalize + sha256Hex/sha256Prefixed (packages/core/src/canonical), policyHash, loadPolicyFromYaml -> { policy, policy_hash }, mergeOverrides -> { policy, rejected } (packages/core/src/policy). packages/core depends on @adgate/schemas (workspace:*) and resolves it from packages/schemas/dist, so turbo builds schemas first; after editing schemas run `pnpm build` before running core tests directly with vitest. fixtures/canonical-json.json holds the canonical JSON vectors and hash.test.ts pins the default policy hash; both are cross-language vectors for the Python SDK.
- Policy engine (S05): POLICY_RULES (the seven docs/policy.md rules in order), PolicyRule, PolicyDecisionResult, PolicyDecision {rule, result, detail?} and CapState {session_count, day_count, turns_since_last | null} are Zod schemas in packages/schemas/src/policy-decision.ts and registered in CONTRACT_SCHEMAS (S14's AuditRecord.policy_decisions must be z.array(PolicyDecision)). evaluatePolicy({ classification, user, policy, capState, surface }) -> { allowed, reason, decisions } lives in packages/core/src/policy/evaluate.ts (pure; the gateway reads capState from cap_state and passes it in; turns_since_last is null until the first ad in a conversation). EU expansion: packages/core/src/policy/eu-members.ts (EU_MEMBER_STATES) and regions.ts (expandRegions, isRegionAllowed). The engine does NOT read policy.sensitive_detection (the classifier applies it when merging rule and LLM sensitive signals) and always records competitor_exclusions as pending (mediation resolves it against advertiser_domain). evaluate-purity.test.ts fails if anything under packages/core/src/policy imports node:*, the gateway or an I/O library.
- Rules classifier (S06): packages/core/src/classify/rules. classifyByRules(text) -> RulesResult = Classification (method 'rules', prompt_version RULES_VERSION) plus `matches` (dictionary terms only, never message text); strip matches before persisting (`const { matches, ...classification } = result`). Keyword data is one TS module per category under data/sensitive/<category>.ts and data/commercial/<category>.ts registered in data/index.ts (Record types make a missing taxonomy category a compile error); every phrase is written in normalized form (data.test.ts enforces normalizeText(p) === p: lowercase, no punctuation, contractions expanded to 'do not' / 'can not', alias targets such as 'bitcoin' not 'crypto'); aliases in data/aliases.ts, intent phrases in data/intent.ts, informational phrases in data/informational.ts, scoring constants in data/scoring.ts. RULES_VERSION = sha256:<hex> over canonicalize(RULES_DATA), so any data edit changes it. Fixture contract (fixtures.test.ts): every s* case is caught by rules alone, no c*/l* case gets a sensitive flag, and c001 plus every software.devtools.* case is servable by rules alone (confidence >= 0.7, intent in bounds, listed category). S08 joins messages (last 4 / 4,000 chars) before calling classifyByRules and unions its sensitive flags with the LLM's. packages/core/src/classify/rules/data/ is prettier-ignored (hand-formatted lists).
- LLM classifier (S07): packages/core/src/classify/llm. LlmClassifier { classify(text, { signal? }) -> Promise<LlmClassifyResult> } where LlmClassifyResult is { ok: true, classification (method 'llm', prompt_version PROMPT_VERSION), latency_ms } | { ok: false, reason: 'timeout' | 'parse' | 'invalid' | 'http' | 'network' | 'aborted', latency_ms, detail? } and never throws. OpenAiCompatibleClassifier({ baseUrl, apiKey, model, timeoutMs = 400, fetch = globalThis.fetch }) POSTs <baseUrl>/chat/completions (temperature 0, response_format json_object, system CLASSIFIER_PROMPT + user text) and races a hard timeout. CLASSIFIER_PROMPT is built from the taxonomy consts; PROMPT_VERSION = sha256:<hex> of the prompt text (computePromptVersion). LlmOutput (the raw model JSON: commercial_intent, categories: string[], sensitive: string[], confidence) lives in @adgate/schemas classification.ts, NOT in CONTRACT_SCHEMAS; core clamps numbers to [0,1] then normalizes lists to the taxonomies (normalizeCategories -> ['general'] when empty, normalizeSensitive). Tests use FakeLlmClassifier(script) with script = fixed result | (text) => result | 'timeout' | 'parse' | 'throw' (throw rejects on purpose), fakeLlmSuccess/fakeLlmFailure builders, and fakeLlmFromFixtures(cases) built from fixtures/classify-fixtures.json cases (the test reads the file; core never touches fs). Every llm test stubs globalThis.fetch to reject, so nothing can reach the network. The gateway must build the config from CLASSIFIER_BASE_URL / CLASSIFIER_API_KEY / CLASSIFIER_MODEL / CLASSIFIER_TIMEOUT_MS (already in .env.example); core never reads env.

## 2026-09-02 S01 Monorepo scaffold
Built: pnpm workspaces + Turborepo 2, TypeScript 5.9 strict ESM (NodeNext), Vitest 3 (root projects runner + per-package vitest.config.ts), ESLint 10 flat config + typescript-eslint 8 + Prettier 3, tsup builds. Packages @adgate/schemas, @adgate/core, @adgate/gateway, @adgate/sdk each have package.json, tsconfig.json, src/index.ts (exports packageName) and a passing smoke test. Verified from a clean copy with pnpm install --frozen-lockfile.
Decisions: All tooling devDependencies live at the workspace root only; packages declare none (pnpm exposes root bins to workspace scripts). Relative imports inside src use the .js extension (NodeNext); vitest and tsup resolve them. Tests live in src/**/*.test.ts and are typechecked by tsc --noEmit (tsconfig include: src). build = tsup src/index.ts --format esm --dts; the exports map points at dist, and turbo test/typecheck dependsOn ^build so cross-package imports resolve once packages depend on each other. pnpm test/typecheck/lint run per package through turbo; `pnpm exec vitest run` at the root runs everything via projects. ESLint bans default exports inside packages/*/src (CLAUDE.md: named exports only); tool config files are exempt. `pnpm lint` is ESLint only; `pnpm format:check` is Prettier and is a separate gate. prd.json, fixtures/, docs/ and *.md are prettier-ignored on purpose (hand-formatted, contract docs).
Gotchas: ESLint 9 is end of life (pnpm prints deprecated) so the repo is on ESLint 10 + @eslint/js 10. `turbo run dev` with no package dev scripts exits 0 with zero tasks, so no placeholder dev script exists; gateway db:migrate is the only placeholder script (allowed until S16). packageManager must be an exact version (pnpm@9.15.9) for turbo and corepack. Prettier reformatted docker-compose.yml, examples/*.json|yaml and pnpm-workspace.yaml (quotes and wrapping only; no semantic change). For S02: CI should use Node 20 and `pnpm install --frozen-lockfile`; the compose default host port 5432 is correct in CI (5433 is only this dev machine, see Codebase notes).
Ideas: root tsconfig.json for editor support of root config files; coverage thresholds once real logic lands.

## 2026-09-02 S02 Local infra, CI, license, README
Built: .github/workflows/ci.yml (push + pull_request; Node 20; pnpm pinned via packageManager; frozen-lockfile install; postgres:16 service container with a psql step that creates adgate_test; typecheck, lint, test with DATABASE_URL/DATABASE_URL_TEST on 5432). LICENSE fetched verbatim from https://www.apache.org/licenses/LICENSE-2.0.txt. README rewritten around the required headings "What adgate is", "What it is not", "Quickstart" (placeholder until S39), "Repo layout". .env.example now has a comment for every variable including the nine required ones. docker-compose.yml and scripts/db/init.sql were already complete from the starter and are unchanged (host port stays ${PGPORT_HOST:-5432}).
Decisions: pnpm/action-setup gets no explicit version so it reads package.json packageManager (pnpm@9.15.9); bump only that field. adgate_test in CI is created with psql plus a WHERE NOT EXISTS guard and \gexec (idempotent on re-runs) because service containers cannot mount the compose init script. DATABASE_URL and DATABASE_URL_TEST are job-level env so every step, not just test, sees them. Workflow adds permissions: contents: read and a per-ref concurrency group that cancels superseded runs. No dependencies added.
Gotchas: There is no GitHub remote, so the workflow was verified locally, not on GitHub: the YAML was parsed with js-yaml (transitive dep in node_modules/.pnpm) and checked for triggers, node-version 20, service image, ports and job env; the psql step was run verbatim against the docker Postgres on 5433 after dropping adgate_test (created it, second run was a no-op, exit 0); then the exact command sequence pnpm install --frozen-lockfile, pnpm typecheck, pnpm lint, pnpm test ran green with the job env exported, and pnpm format:check passed. psql -c cannot mix SQL with \gexec, so the step feeds psql through a heredoc. zsh treats `status` as a read-only variable; do not use it as a shell variable name in scripts. Prettier formats .github/workflows/*.yml (singleQuote), so run pnpm format:check after editing the workflow. pnpm install --frozen-lockfile added four empty `packages/<name>: {}` importer entries to pnpm-lock.yaml that S01 had not committed (packages have no deps yet); committed here so CI's frozen install is stable, and a second frozen install leaves the lockfile untouched.
Ideas: add pnpm format:check as a CI step once the loop stabilises; cache turbo outputs in CI with actions/cache keyed on the lockfile; CONTRIBUTING.md and a NOTICE file when third-party code is vendored; a README badge once a GitHub remote exists.

## 2026-09-02 S03 Zod schemas for the API contract
Built: packages/schemas implements docs/api.md with zod 4.5.4. Modules: taxonomy.ts (SENSITIVE_TAXONOMY, CATEGORIES_TAXONOMY, SensitiveCategory, ContentCategory), common.ts (AppId, AuditId, CreativeId, Sha256Hash, IsoTimestamp, MessageRole, Message, User, SurfaceType, Surface, ErrorResponse, HealthResponse), classification.ts (ClassificationMethod, Classification), creative.ts (DemandSource, Creative), suppress-reason.ts (FIXED_SUPPRESS_REASONS, SENSITIVE_CATEGORY_REASONS, SUPPRESS_REASONS, sensitiveCategoryReason(), SuppressReason), evaluate.ts (Decision, EvaluateRequest, EvaluateResponse), events.ts (AttestRequest, EventType, EventRequest), verify.ts (VerifyCheckName, VerifyCheck, VerifyResponse), json-schema.ts (CONTRACT_SCHEMAS, toJsonSchemaObject, renderJsonSchema(s), toJsonSchema(outDir?) which writes packages/schemas/json/<Name>.schema.json), gen-json.ts (the gen:json script). 28 JSON Schema files committed. Every TS type is z.infer; index.ts re-exports all modules. 68 tests: all seven docs/api.md bodies verbatim (api-examples.test.ts), taxonomy equals the fixture, SuppressReason accepts the 7 fixed reasons plus sensitive_category:<taxonomy name> and rejects sensitive_category:unknown, EvaluateRequest rejects both messages and context_summary absent, serve/suppress consistency, JSON files byte-identical to a fresh render (verified to fail on a stale file and to be idempotent).
Decisions: zod v4 native z.toJSONSchema (target draft-2020-12, io 'input', unrepresentable 'throw'); no zod-to-json-schema. io 'input' because zod strips unknown keys instead of rejecting, so files carry no additionalProperties:false and stay forward compatible for the Python SDK. JSON files: keys sorted at every level except each `properties` map, which keeps declaration (contract) order; two-space indent; trailing newline; title equals the export name; everything inlined (no $defs) so each file is self-contained. Refinements zod cannot express are attached with .meta() and merged verbatim: EvaluateRequest anyOf required messages|context_summary, EvaluateResponse allOf if/then (serve needs creative object and null reason, suppress needs null creative and string reason). SuppressReason is one flat z.enum (7 fixed + 9 sensitive_category:<name> built from SENSITIVE_TAXONOMY) so the JSON Schema is an explicit enum; core should build reasons with sensitiveCategoryReason(name) for exact typing. Classification.categories is z.array(ContentCategory) and sensitive is z.array(SensitiveCategory), both taxonomy-enforced: the classifier story must normalise LLM output to the taxonomy (unknown category -> general, unknown sensitive flag -> treat as failure/suppress). ClassificationMethod is z.enum(['llm','rules']); prompt_version is any non-empty string; extend the enum only if an error path truly needs it. EvaluateRequest: app_id must start with app_ (prefixed ULID convention; only the prefix is checked because doc examples are elided); user.tier any non-empty string; user.region optional ^[A-Z]{2}$; locale and user_hash optional strings; messages optional but min 1 item when present; context_summary optional min 1; refine requires at least one (issue path ['messages']); surface.placement literal after_answer, max_creatives optional int >= 1 (no default, so input and output types match); policy_overrides is z.record(z.string(), z.unknown()) and S04 should tighten it to a partial PolicyConfig. Unknown keys are stripped, not rejected. EvaluateResponse keeps classification and audit_id required even for reason 'error' (the contract shows them always present): the gateway must synthesise a zeroed classification and still write an audit record on error paths. creative.url is a non-empty string, not z.url(), because the documented example https://<gateway>/c/... is not a parseable URL; Creative.body may be empty, other text fields min 1; DemandSource is direct|affiliate|koah|gravity from docs/policy.md. MessageRole is system|user|assistant|tool. IsoTimestamp = z.iso.datetime() accepts only trailing-Z UTC. Sha256Hash requires the sha256: prefix (AttestRequest.model_output_hash); user_hash stays loose because the doc example is "optional sha256". VerifyCheckName is the eight docs/audit.md checks in order. HealthResponse and ErrorResponse added beyond the story list so every api.md body has a schema. Removed the scaffold packageName export from @adgate/schemas. Dependencies added: zod ^4.5.4 in @adgate/schemas (stack list) and tsx ^4.23.13 at the workspace root so TypeScript dev scripts (gen:json now, seed and migrate scripts later) run without a build step. packages/schemas/json/ is prettier-ignored and listed in package.json files.
Gotchas: Prettier needed a second --write pass on common.ts (member-chain formatting is not idempotent on the first pass); always finish with pnpm format:check. z.toJSONSchema adds maximum 9007199254740991 to .int() and a long regex pattern to z.iso.datetime(); harmless. toJsonSchema() imports node:fs statically from the package entry; if a browser bundle ever imports @adgate/schemas at runtime, move it to a subpath export. `z.string().startsWith()` renders as pattern ^prefix.* in JSON Schema. The agent cwd resets between shell calls; use absolute paths for every file edit.
Ideas: subpath export @adgate/schemas/json-schema to keep node:fs out of the main entry; the gateway /openapi.json (S-gateway) can be assembled from CONTRACT_SCHEMAS via z.toJSONSchema with $defs; PolicyConfig (S04) and AuditRecord (audit story) should be registered in CONTRACT_SCHEMAS so they get JSON files and the staleness test; a coverage threshold for packages/schemas.

## 2026-09-02 S04 PolicyConfig schema, YAML loader, policy hash
Built: packages/schemas: policy-parts.ts (default-free leaf schemas PolicyTier, PolicyRegion, PolicyThreshold, PolicyCap, PolicyRetainDays, AdvertiserDomain, SensitiveDetection, DisclosureLabel/Position/Style, AffiliateNetwork), policy.ts (FrequencyCaps, Disclosure, Privacy, Regions, DemandEntry, PolicyConfig with every docs/policy.md default, PolicyConfigInput), policy-overrides.ts (DemandEntryOverride, PolicyOverrides, OverrideRejection), policy-loader.ts (PolicyValidationError, parsePolicy, parsePolicyYaml). EvaluateRequest.policy_overrides tightened from a loose record to PolicyOverrides. PolicyConfig and PolicyOverrides registered in CONTRACT_SCHEMAS (30 JSON files now). packages/core: canonical/canonicalize.ts, canonical/sha256.ts, policy/hash.ts (policyHash), policy/load.ts (loadPolicyFromYaml), policy/merge.ts (mergeOverrides); scaffold packageName export removed; PolicyValidationError re-exported. fixtures/canonical-json.json added. Tests: schemas 114 (65 new), core 59.
Decisions: Split keeps the dependency direction schemas <- core: schemas owns validation, defaults and YAML parsing (the `yaml` package lives in @adgate/schemas, matching docs/architecture.md "PolicyConfig loader"); core owns canonicalize, policyHash, loadPolicyFromYaml (= parsePolicyYaml + policyHash) and mergeOverrides. Defaults: version is z.literal(1) defaulting to 1; app_id is required and is a free non-empty string (docs use slugs like my-chat-app, not the app_ prefixed AppId); demand defaults to the four documented entries, affiliate entries require network (no default), enabled defaults true per entry; all defaults are factories so every parse returns fresh objects. Validation: z.strictObject at every level; blocked_categories values are SensitiveCategory (the suppress reason sensitive_category:<name> only exists for the sensitive taxonomy); regions.allow matches /^[A-Z]{2}$/ (EU matches, no ISO list check, same rule as User.region); empty serve_to_tiers and regions.allow are accepted (serve nobody = fail closed). JSON Schema: PolicyConfig and PolicyOverrides carry additionalProperties:false (json-schema.test.ts permits it for those two only) plus `contains: {const: self_harm}` and an if/else allOf for the two refinements. Errors: parsePolicy, parsePolicyYaml and loadPolicyFromYaml throw PolicyValidationError with issues [{path, message}] (dotted paths, [n] for indexes, '' for root) and a message "Invalid policy:\n  - path: message"; YAML syntax errors become one root issue prefixed "YAML syntax: ". YAML 1.2 core schema: `yes` stays a string, duplicate keys, multi-document and empty documents are errors. policy_hash = "sha256:" + sha256(canonicalize(policy)) with no re-parse, so only pass fully defaulted PolicyConfig values (parsePolicy, parsePolicyYaml and mergeOverrides outputs). Canonical JSON: hand-assembled string (JSON.stringify per scalar), keys sorted by UTF-16 code unit at every depth (RFC 8785 order; integer-like keys stay lexicographic), undefined properties dropped, any non-JSON value (NaN, Infinity, Date, Map, bigint, function, undefined array element) throws TypeError. Golden: the docs default policy for app_id my-chat-app hashes to sha256:f7443a7f6a44e611bf4deb2d6517341e680f3bae153e7bb3f805ac1d6c0603ae (pinned with its canonical string in packages/core/src/policy/hash.test.ts). mergeOverrides (tighten only, equal values are silent no-ops, inputs never mutated): serve_to_tiers and regions.allow are intersected with the stored list and items the override would add are rejected while its removals still apply; allow_paid_tiers true->false applies and filters serve_to_tiers to free, false->true rejected; blocked_categories and competitor_exclusions are unioned in stored-then-override order, never rejected; sensitive_detection balanced->strict only; min_commercial_intent and min_confidence up only; per_session and per_user_per_day down only; min_turns_between UP only (more turns between ads is stricter, even though docs say "lower caps"); demand entries with enabled:false disable matching stored entries (affiliate without network matches every affiliate entry), enabling a disabled source or naming one the policy lacks is rejected; privacy.store_raw_text true->false only; version, app_id, disclosure.* and privacy.retain_days are per-app settings and any change is rejected. Rejection paths are dotted (frequency_caps.per_session, demand[1]); these become override_rejected notes in the audit story. mergeOverrides takes an already validated PolicyOverrides (EvaluateRequest validates it) and does not re-validate. Shared test fixtures live in non-test modules (packages/schemas/src/policy.fixture.ts, packages/core/src/policy/merge.fixture.ts) because importing a *.test.ts re-registers its tests in the importer. Dependencies added: yaml ^2.9.0 in @adgate/schemas (YAML 1.2 parser, zero deps, actively maintained; needed for parsePolicyYaml); @adgate/schemas workspace:* in @adgate/core (pnpm add writes workspace:^, normalised by hand to workspace:* per the convention).
Gotchas: zod 4 `.default(value)` hands back the same object on every parse, so object and array defaults must be factories. `.partial()` on a schema whose fields carry defaults still fills them in, which is why PolicyOverrides is built from default-free leaf schemas instead of PolicyConfig.partial(). zod 4 runs superRefine only when the shape has no structural issue (missing key, wrong type); value issues (ranges, patterns) co-report with the refinement issues, so tests must not expect the self_harm issue next to a missing key. Function-form defaults on a discriminated-union array need an explicit return type (`(): DemandEntry[] =>`) or TS widens the literals. Object.keys enumerates integer-like keys first, so canonical JSON cannot be JSON.stringify over a re-keyed object. `printf '%s' '<canonical json>' | shasum -a 256` derives golden hashes without a build. Prettier changed line wrapping in eight files after the first write; run it before the gates.
Ideas: a defaultPolicy(app_id) helper; validate regions.allow against the ISO 3166 list and add the EU member-state expansion the policy engine needs; a root `pnpm gen:json` alias; deduplicate list fields inside PolicyConfig; the audit story can reuse sha256Prefixed and canonicalize for record_hash and content_hash.

## 2026-09-02 S05 Policy engine
Built: packages/schemas/src/policy-decision.ts (POLICY_RULES, PolicyRule, PolicyDecisionResult pass|fail|pending, PolicyDecision, CapState; PolicyDecision, PolicyRule and CapState registered in CONTRACT_SCHEMAS, 33 JSON files now). packages/core/src/policy: eu-members.ts (EU_MEMBER_STATES, isEuMemberState), regions.ts (EU_REGION_TOKEN, expandRegions, isRegionAllowed), evaluate.ts (evaluatePolicy, EvaluatePolicyInput, PolicyEvaluation), evaluate.fixture.ts (serveInput, defaultPolicy, SERVE_CLASSIFICATION, FREE_US_USER, FRESH_CAP_STATE, CHAT_SURFACE). Tests: evaluate.test.ts (order, first failure wins, tiers, blocked categories, thresholds, competitor pending, surface), evaluate-caps.test.ts (every cap at its boundary), evaluate-purity.test.ts (import grep), regions.test.ts (EU data, expansion, region rule). Totals: schemas 121 tests (+7), core 110 (+51).
Decisions: Rules run from a Record<PolicyRule, RuleCheck> iterated in POLICY_RULES order; every rule appends exactly one decision and reason is the first fail. Pass entries carry no detail except frequency_caps, which always renders `session=<n>/<cap> day=<n>/<cap> turns_since=<n>` (day=n/a without user_hash, turns_since=none when null) so the docs/audit.md example is reproduced byte for byte; fail entries carry a short detail (tier=paid not in serve_to_tiers, tier=paid requires allow_paid_tiers, region missing, region=BR not in regions.allow, health in sensitive / health in categories, confidence=0.69 min=0.7, commercial_intent=0.59 min=0.6). competitor_exclusions is always { rule, result: 'pending' } with no detail. Details never contain message text. serve_to_tiers re-checks allow_paid_tiers for any non-free tier on top of the schema guarantee (CLAUDE.md non-negotiable). regions: EU expands to the 27 member states (ISO codes, GR not EL); a missing user.region fails closed; a user.region of EU never matches because the token only has meaning inside the policy. blocked_categories iterates policy.blocked_categories in policy order and checks classification.sensitive then classification.categories as plain strings (the content taxonomy has no sensitive names today; the check is defensive as the doc requires both), appending self_harm if a hand-built policy dropped it. Thresholds use `score >= min` and caps use `count < cap` so NaN fails closed: equal to a threshold passes, equal to a cap fails, turns_since_last equal to min_turns_between passes, null passes, per_user_per_day applies only when user.user_hash is a non-empty string. policy.sensitive_detection is not read by the engine: it decides how the classifier merges rule and LLM sensitive signals into classification.sensitive, so the classifier stories own it. surface is accepted and ignored (placement is schema-enforced). evaluatePolicy never throws on typed inputs and does not try/catch malformed ones; the gateway's evaluate wrapper maps any throw to decision suppress reason error. EvaluatePolicyInput and PolicyEvaluation are TS interfaces in core (like MergedPolicy); only the shapes shared with the audit record are Zod. No dependencies added.
Gotchas: exactOptionalPropertyTypes means PolicyDecision.detail must be omitted rather than set to undefined, so decisions are built with a ternary, not a spread. Prettier's wrapping pushed the first evaluate.test.ts past 300 lines; the tests are now split by rule family. A test module that imports a *.test.ts re-registers its tests, so shared inputs live in evaluate.fixture.ts. The purity test walks relative imports from evaluate.ts and asserts the only bare specifier is @adgate/schemas; importing ../canonical (node:crypto) from the engine would fail it on purpose.
Ideas: the gateway should derive turns_since_last as current turn index minus cap_state.last_turn_index (null when no ad yet) and reset day_count on a new UTC day; a renderDecisions helper for the dashboard; validate regions.allow against the ISO 3166 list at parse time; a table-driven fixture file (fixtures/policy-cases.json) so the Python SDK can replay the same cases.
## 2026-09-02 S06 Rules classifier
Built: packages/core/src/classify/rules: normalize.ts (normalizeText: NFKC, lowercase, apostrophes deleted, other punctuation to spaces, whitespace collapsed, whole-token aliases via a Map), match.ts (padded whole-token phrase matching, compiled regex patterns, containsPhrase), score.ts (noisy-OR intent, evidence-count confidence, sensitive confidence), registry.ts (data compiled once, taxonomy order), classify.ts (classifyByRules -> RulesResult), version.ts (computeRulesVersion, RULES_VERSION), types.ts, index.ts. Data: data/sensitive/{health,finance,politics,legal,adult,gambling,weapons,religion,self-harm}.ts, data/commercial/<each of the 18 content categories>.ts, data/aliases.ts, data/intent.ts, data/informational.ts, data/scoring.ts, data/index.ts (SENSITIVE_RULES, COMMERCIAL_RULES, RULES_DATA). Tests: fixtures.test.ts (28 s* cases caught with confidence >= 0.9 and intent <= 0.2, 39 c*/l* cases with no sensitive flag, 7 quickstart devtools cases, aggregate floor), classify.test.ts, normalize.test.ts (case, NFKC, punctuation, contractions, every alias), data.test.ts (hygiene), version.test.ts, purity.test.ts. Core 325 tests (+215). @adgate/core now exports classifyByRules, normalizeText, containsPhrase, RULES_VERSION, computeRulesVersion, RULES_DATA and the rules types.
Decisions: RulesResult is a flat Classification with method 'rules' plus `matches` {sensitive, commercial, intent, informational}; matches hold only dictionary terms and pattern sources, so nothing from the message text leaks, but S08 must still drop `matches` before persisting. QUICKSTART WITHOUT AN LLM KEY: c001 and every software.devtools.* fixture case (c001-c006, c024) reach confidence >= 0.7, commercial_intent inside [intent_min, intent_max] and one of categories_any by rules alone (c001 scores intent 0.992 / confidence 0.95 / categories [database, hosting]); fixtures.test.ts pins this per case, so the default policy (min_confidence 0.7, min_commercial_intent 0.6) serves on rules only. Sensitive is two-tier: any strong phrase or pattern flags the category (confidence 0.9, 0.95 when a second term corroborates); weak phrases flag only when two distinct ones match (confidence 0.75); a single weak hit is reported in matches.sensitive with flagged:false as a hint for S08. Every s* fixture case is a strong match. A sensitive flag caps commercial_intent at 0.2; self_harm forces 0; sensitive categories come out in taxonomy order. Intent = noisy-OR of matched intent phrase weights (strong 0.45, medium 0.3, weak 0.15) plus one category signal (product phrase 0.7, topic phrase 0.2) minus 0.15 per informational phrase (explain, how do i, translate, fix this...), clamped and rounded to 3 decimals. Confidence = 0.3 + 0.15 per distinct matched term (+0.15 when a product matched), max 0.95; no evidence gives 0.3, which fails min_confidence (fail closed). Categories are ordered by score (product 1, topic 0.5), ties in taxonomy order; no hit gives ['general'] (matches the S03 unknown->general rule). Remaining 32 non-devtools serve and low-intent cases: 32 of 32 satisfy their intent bounds and categories by rules alone; the aggregate test prints the number and asserts a floor of 30 so keyword edits do not fail on a single case. No fixture exception was needed: zero sensitive false positives on every c* and l* case. Terms deliberately left out or demoted to weak to protect dev and low-intent text: budget and dollars (finance; they are buying signals), convert (religion; unit conversion), miles (flights), std, stroke and side effects (health; std::vector, CSS stroke, pure functions), bare course, render, modal, cursor, ram, cpu, mouse, max, prime, show, stream, suite, tools, sheets; "insurance" is weak with the specific kinds strong, so pet insurance and travel insurance are products, not finance. RULES_VERSION hashes the canonical JSON of the whole RULES_DATA (phrases, patterns, aliases, intent, informational, scoring), computed once at module init; the test mutates a structuredClone to prove it changes and that key order does not matter. Phrase data must be written in normalized form (data.test.ts) and alias targets are never alias keys (normalize.test.ts). Patterns are regex sources (strings) compiled without flags at init; they count as strong/product hits. packages/core/src/classify/rules/data/ is prettier-ignored because Prettier puts one string per line and would push the lists toward the 300-line limit; purity.test.ts asserts every file under rules/ stays under 300 lines and that the import graph reaches only @adgate/schemas and node:crypto. No dependencies added.
Gotchas: The Bash tool rejects a command whose text contains certain escape sequences ("\r\n" inside a heredoc was refused as hidden control characters); the Write tool is the fallback. A tsx script outside packages/* cannot resolve @adgate/schemas (exports map), so run dev scripts from inside the package. vitest it.each over object arrays supports $id in test names. The fixture path from packages/core/src/classify/rules is ../../../../../fixtures/classify-fixtures.json. Prettier only re-wrapped three files; the data directory is ignored on purpose.
Ideas: light stemming so plurals need not be listed by hand; per-term weights in the data files; a negative-context list to unflag phrases like nerf gun or glue gun; a fixture of joined multi-turn conversations once S08 lands; expose matches in the dashboard as a "why suppressed" view; a script that prints rules-only results for the whole fixture to tune lists.

## 2026-09-02 S07 LLM classifier with timeout and versioned prompt
Built: packages/core/src/classify/llm: types.ts (LlmClassifier, LlmClassifyResult/Success/Failure, LlmClassifyOptions, LlmClassifierConfig, the LlmFetch/LlmFetchInit/LlmFetchResponse slice of fetch, LLM_FAILURE_REASONS, DEFAULT_LLM_TIMEOUT_MS = 400), prompt.ts (CLASSIFIER_PROMPT, computePromptVersion, PROMPT_VERSION), output.ts (parseLlmContent, toLlmClassification, normalizeCategories, normalizeSensitive), openai.ts (OpenAiCompatibleClassifier, CHAT_COMPLETIONS_PATH, buildChatCompletionsUrl, buildChatCompletionsBody), fake.ts (FakeLlmClassifier, fakeLlmSuccess, fakeLlmFailure), fixtures.ts (fakeLlmFromFixtures, fixtureLlmResult, unknownTextLlmResult, FIXTURE_LLM_CONFIDENCE, ClassifyFixtureCase/Expect), index.ts; all re-exported from @adgate/core. packages/schemas/src/classification.ts gains LlmOutput. Tests: prompt.test.ts (sha256 of the exported text, changes on edit, lists every taxonomy value), output.test.ts, openai.test.ts (timeout with fake timers incl. a fetch that ignores abort and a slow body, parse, invalid, http, network, aborted, default globalThis.fetch wiring), openai-request.test.ts (URL, bearer, model, temperature, response_format, messages, happy path with taxonomy normalisation), fake.test.ts (modes, call recording, fixture script over all 67 cases), purity.test.ts. Core 381 tests (+56), schemas 125 (+4).
Decisions: LlmOutput is a Zod schema in @adgate/schemas (not CONTRACT_SCHEMAS) because core imports only @adgate/schemas and node:crypto and has no zod dependency; the llm purity test pins that import graph. Numbers are clamped into [0,1] BEFORE validation (1.02 -> 1, -0.1 -> 0), non-numbers, missing keys and non-string list items are reason 'invalid'; list values are trimmed, lowercased, spaces and hyphens become '_' ("Self-Harm" -> self_harm), unknown values are dropped, duplicates removed, empty categories -> ['general']. Reasons: response body not JSON or no choices[0].message.content string -> parse; content not JSON -> parse; content fails LlmOutput -> invalid; non-2xx -> http (detail "HTTP <status>"); fetch rejection, synchronous throw or body read failure -> network (detail "<name>: <message>" cut at 200 chars); timer -> timeout; caller signal -> aborted (before the call: latency_ms 0, fetch not called). detail never carries message or model text: JSON.parse messages quote the input in Node 20 so a fixed string is used, and schema issues are reported as "path: code". Timeout is Promise.race between the request and a timer that also aborts an internal AbortController, so classify settles at timeoutMs even if the transport ignores the signal; the caller's signal is linked to the same controller and unlinked in finally so no timers or listeners leak. latency_ms is Date.now() based (integer, fake-timer friendly). Request: POST <baseUrl with trailing slashes trimmed>/chat/completions, Content-Type application/json, Authorization: Bearer <apiKey> omitted when apiKey is '' (local endpoints), body { model, temperature: 0, response_format: { type: 'json_object' }, messages: [system CLASSIFIER_PROMPT, user text] }, no max_tokens (some compatible endpoints reject it; the timeout bounds latency). Content wrapped in a markdown fence is unwrapped before parsing. The default fetch is a wrapper around globalThis.fetch (not a captured reference) so vi.stubGlobal works after construction; every llm test stubs it to reject. FakeLlmClassifier: 'timeout' returns reason timeout with latency 400, 'parse' returns reason parse, 'throw' rejects with "FakeLlmClassifier: throw mode" so S08 can prove a throw becomes rules-only; calls[] holds every text, plus callCount and reset(). fakeLlmFromFixtures: intent = midpoint of [intent_min, intent_max] when both exist, else 0.9 for a sensitive-less case, 0.1 for a sensitive case, 0 for self_harm; categories = normalized categories_any or ['general']; sensitive = expect.sensitive normalized; confidence 0.9; unknown text -> intent 0.1, ['general'], confidence 0.5. Matching is normalizeText exact first, then the longest fixture text contained in the input, so a joined multi-turn conversation still hits its fixture. ClassifyFixtureCase is a TS interface (test data, not a contract). The prompt is paragraphs joined with blank lines, built from CATEGORIES_TAXONOMY and SENSITIVE_TAXONOMY, so a taxonomy edit changes PROMPT_VERSION too; no test pins the current value (sha256:6a94e128...) on purpose. Gateway env wiring belongs to the gateway story (variables already exist in .env.example). No dependencies added.
Gotchas: purity.test.ts greps implementation sources for the literal process.env, so even a doc comment mentioning it fails. vi.fn(async () => ...) infers zero parameters and mock.calls[0][1] is then a tsc error; type the fake as vi.fn<LlmFetch>(impl). Joining fixed-width prompt lines with '\n' split "never answer the user" across lines and failed the wording test; hence the paragraph() helper. Vitest fake timers also mock Date, so latency_ms in timeout tests equals timeoutMs exactly. Cross-package: after adding LlmOutput to schemas, run `pnpm --filter @adgate/schemas build` before running core's vitest directly. The Write tool emitted a NUL byte in place of a single-space string literal (' ') in output.test.ts; tests still passed but git showed the file as `Bin` in `git show --stat`. Check the commit stat for Bin entries on .ts files and grep for \x00 before committing.
Ideas: an Anthropic Messages API LlmClassifier as a second implementation; optional max_tokens in the config; return the model's usage block for cost dashboards; one retry on 429 within the remaining deadline; pre-serialize the system message; a NEEDS-LLM-KEY smoke script that hits a real endpoint with the fixture set and prints precision, run manually only.
