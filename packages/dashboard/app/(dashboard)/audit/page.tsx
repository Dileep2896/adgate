import { Placeholder } from '@/components/placeholder';

const AuditPage = () => (
  <Placeholder
    title="Audit"
    story="S34"
    summary="Search audit records by app, date range, decision and reason; the detail view runs verify() live and lists every check against the chain."
  />
);

export default AuditPage;
